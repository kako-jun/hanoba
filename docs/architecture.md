# architecture

hanoba のアーキテクチャ（非ビジュアル）。見た目は `DESIGN.md`、製品仕様・画面挙動は `docs/spec.md` を参照。
本書は元 DESIGN.md の §2 アーキテクチャ・§3 実装方針・§6 データ（Nostr）契約を集約したもの（session765 の3層分割・内容は逐語移設）。

## 2. アーキテクチャ

- **自前バックエンドなし。** 状態は全部 Nostr に乗る。投稿は **mypace 経由でなく共有 Nostr 空間へ直接**行う。「mypace のスキン」の実体は **同じ Nostr タグ規約を使うこと**。
  - 補足（#28・実装確認）: mypace 自体は Workers＋D1 の API を持つが、**Hanoba はそれを使わない**。投稿（kind:1）・削除（kind:5）・プロフィール（kind:0）・画像アップロード/削除（nostr.build NIP-96）は**全てクライアント側**で完結し、バックエンド不要を維持する。
- **アカウント（#28）**: nsec はブラウザ生成・localStorage 保存（`getOrCreateSecretKey`）。**ユーザー名（kind:0）を入れたら投稿できる**（見るだけ→参加）。**既存ユーザーは nsec を持ち込める**（`AccountName` の「すでにアカウントをお持ちですか？」→ `importNsec` → 既存 kind:0 全体（名前＋picture/about/websites）を `fetchMyProfileResilient` で引き継いで控えにシードし reload）。取得は bounded retry で行う（単発だと接続直後に websites を載せた版を取りこぼし、控えに `websites:[]` を焼いてしまう＝#93）。mypace 等のユーザーがアカウントを無駄に増やさない。鍵は端末（localStorage）にのみ保存。
- **削除＝写真と一蓮托生（#28）**: 投稿削除は ①NIP-09 `kind:5` を publish（mypace/Nostr 上で不可視化）＋②**nostr.build の画像を本人の鍵で NIP-96 削除**（`DELETE /api/v2/nip96/upload/<sha256>`・NIP-98 認証）。「自分の植物」`/me` から行う。D1 を持つ mypace と同じ"消せる一芸"を backendless で再現（画像 URL は投稿本文に内包されるので upload 履歴 DB も不要）。鍵を失うと過去投稿は消せない＝鍵の重要性に注意。
- **編集＝確認つき再投稿（#300・mypace 由来）**: Nostr はイベント不変なので「編集」＝**新しい投稿を publish して旧イベントを NIP-09 `kind:5` で削除**する（`client.editPost`＝**新規 publish → 旧削除**の順。先に消さない＝publish 失敗で投稿が消える事故を防ぐ）。**写真は本文 URL を再利用する（再アップロードしない）**ので、`deletePost` と違い nostr.build の実体は消さない（消すと再投稿側の URL が死ぬ）。**いいね・コメントは旧イベント id に紐づくため引き継がれない**＝`/me`（`MyGrid`）の編集ボタン→`EditPost` モーダルで本文（画像 URL を除いた全文＝`#`タグも本文の一部として直す）を直し、保存時に**「いいね N・コメント M は引き継がれません」確認を1段挟んで**から実行する。写真の追加/削除/差し替えは現状なし（本文編集のみ・follow-up）。
- **市民の活動スタッツ（#272・脱ゲーム化）**: `/me`（`MyGrid`）に**活動スタッツ節**（`CitizenStats`）を出す。**全て t:hanoba 投稿からのクライアント集計＝backendless・新たな身バレ無し**（公開投稿を数えるだけ・ステラ/閲覧数は不採用）。項目＝**投稿数・写真枚数（imageUrls 合計）・育てた品種数＋一覧・居住日数（最古投稿から・"在籍"でなく"居住"）**。品種の同定は `tallyVarieties`（`buildFuda` 一本化）を再利用＝ランキングと一致。**育てた品種一覧は横ページング（#388・`VarietyPager`）**＝品種が 1000 種あっても**全件チップを縦に積まない**（巨大 DOM・長大スクロールでプロフィールが破綻し、下の節〔緑の総面積／撮影の草〕に辿り着けなくなるのを防ぐ）。**1 ページ 10 件**にチャンクして**現在ページだけ描画**＝節の高さが常に有界。手帳のように横へめくる（**←→ボタン・キーボード矢印・タッチスワイプ**＋**ぼかしページ遷移**）＝市民手帳（`CityHallBook`）と同じ作法で `carousel.ts` の純関数（`swipeDirection`/`swipeProgress`/`swipeToBlur`）を**共有**する。**端はクランプ（非 wrap）**＝先頭で←・末尾で→は no-op（ボタン disabled）。めくりは `key={page}` の中身だけ blur（枠は固定）し **reduced-motion／1 ページなら blur 無効**。**10 件以下はページャ UI（←→・インジケータ）を出さず従来どおり並べるだけ＝退行なし**。チャンクは純関数 `src/lib/feed/varietyPager.ts`（`chunk`/`pageCount`/`clampPage`・`PAGE_SIZE=10`・テスト済み）。チップの見た目（glass・学名 `SciName`＋和名＋×N・`discoverTagsHref` の discover 絞り込みリンク）は不変。集計は純関数 `computeCitizenStats(posts, catalog, hasName, now)`（`src/lib/feed/stats.ts`・`now` を引数で受け決定的）。**市民レベルの内部段階は 旅人(L0=名乗り前) → 市民(L1) → L2 → …Ln**（`citizenLevelFull`・#272 kako-jun「古参/訪問者という語は使わない・名乗ったら市民のままレベルが上がる」。L2 は名乗り＋5投稿＋居住14日〜＝既存 `citizenLevel`・Ln L3-L6 は複合〔居住×投稿〕で進む）が、**表示ラベル（`citizenLevelLabel`）は #525 で旅人/市民の二値表示に統一**（数字付き Ln はどこにも出さない・#272 の表示方針から転換）。`CitizenStats` は自分/他人共通（**他人の公開プロフ `/u?npub=`・実装済**）で、catalog は動的 import（PostGrid 同型）。**称号/実績バッジは脱ゲーム化で撤去（#272・市長は王様でなく授ける権限はない／未解除 ??? が面積を取って煽るのは hanoba の静けさに合わない）**＝レベルは塗りピル（CTA 見え）でなく**うっすい囲みの静かなステータスラベル**で残すだけ、表示は**事実のスタッツのみ**（投稿/写真/品種/居住＋育てた品種一覧）。称号の置換＝**「緑の総面積」（#310/#344/#387・`GreenArea`）**＝写真の**緑画素割合**を積み「今まで街に足した緑の累計」を**小ラベル「緑100%の写真換算」＋大きく右寄せの値「約○○枚分」**で静かに出す（#439 kako-jun・値を目立たせる＝ラベルと値を左右に分け値を `text-2xl` 右寄せ／i18n は `green.cumulative.label`＋`green.cumulative.value`）。エゴの称号でなく貢献。#344 で**草グリッド・濃淡凡例・先頭写真限定・直近60件上限は廃止**したが、#344 の「全投稿の全写真を素直に累計」は**全数を一斉に DL/デコードして重い**ため、#387 で kako-jun 合意のもと**代表サンプル（上限 N 枚を均等抽出）からの外挿で概算**に変更（1000 枚でも一定コスト・厳密でなくてよい）。画素読みは `crossOrigin=anonymous` の canvas（hanoba は nostr.build=CORS 反射ホストにアップロード＝読める）・他クライアント由来の非CORS画像は tainted で `getImageData` が throw するので try/catch でスキップ（概算・グレースフル）。純粋判定/抽出は `src/lib/feed/green.ts`（`greenRatio`＝緑優勢画素〔g>40 && g>r+8 && g>b+8〕の割合／`cumulativeGreen`＝読めた写真の緑割合の総和＝換算枚数＋読めた枚数／`pickSampleIndices`＝全数→上限 cap の均等抽出インデックス／`estimateCumulativeGreen`＝代表サンプルから全数への外挿概算〔抽出時 sampled:true〕・テスト済み）。画素読みの副作用は `GreenArea.tsx`（上限 cap=120・同時実行 6・URL 単位のセッション内キャッシュ・#387）。**撮影の草（#272 段階4・`ActivityHeatmap`）**＝**いつ・どれだけ撮ったか**を GitHub 風の日別ヒートマップ（週列×7曜日・1マス=1日・濃淡=その日の写真数〔鼓門 JST 暦日〕・**4段階濃淡〔なし/少(1枚)/中(2枚)/多(3枚〜)・#440 kako-jun。0=なしは薄く、緑3段は明瞭に・少が0と紛れない〕**・直近12週・**左に曜日ラベル（#345・**3文字略 `Sun`〜`Sat`・全7行を省略せず固定**〔#345 の「全言語英語・曖昧なし」の意図維持・行=0日〜6土〕）で縦軸を示し、濃淡凡例（少→多の緑3段のみ・「なし」は出さない）は見出し行の右に置く（#440）**・見出し（撮影の草）と「直近N週」は上下中央に揃える（#440）・各週列に7行を貫く控えめな縦トラックを敷き暦週そろえの空白角を「列の中のまだ無い日」と読ませる〔#389〕・kako-jun「縦横わからない/ずれて見える」）＋**連続記録**（現在の連続/最長連続・今日未投稿でも昨日まで続けば生存）で静かに出す。**撮影日基準（#324・kako-jun「毎日撮って週末まとめ投稿する人が報われるべき」）**＝各写真の撮影日（`shot_date` タグ・per-photo）で数え、撮影日が無い写真/投稿は created_at 日にフォールバック（`postActiveDays`）。緑の総面積（緑の貢献量・**全写真からの概算換算枚数**・#344/#387）とは**別軸＝撮影の頻度/継続（per-day）**で補完的。純関数 `src/lib/feed/activity.ts`（`jstDayIndex`/`ymdToJstDayIndex`/`postActiveDays`/`activityHeatmap`/`streaks`/`activityLevel`・テスト済み）。バッジ/手帳連動演出は脱ゲーム化で不採用（事実の可視化だけ）。
- **フロントエンドのみ**: Astro（静的生成）＋ React islands ＋ **PWA**（`@vite-pwa/astro`）。**Cloudflare Pages** にデプロイ（Workers も D1 も不要）。
- クライアントが自分で行う:
  - **鍵** — ブラウザ生成・localStorage 保存・署名（NIP-07 拡張も可）。
  - **画像アップロード** — nostr.build へ直接（NIP-98 認証）。**EXIF はサーバ側で自動削除**（身バレ＝自宅 GPS 漏れ対策が無料で効く）。維持費ほぼゼロ。
  - **投稿・購読** — Nostr リレーへ直接 publish / subscribe。
- **mypace から見える鍵＝タグ**: 投稿に `['t','mypace']` を必ず付ける（mypace タイムライン出現の条件）。詳細は §6。
- **mypace の記録 API への ping は後回し（任意・優先度低）**: ping すれば mypace の通知・スコア（ステラ/通し番号）系にも乗るが、無くても `t:mypace` だけで mypace タイムラインには出る。必要になったら将来。
- **断り書きを常設**: 「Nostr フィードを表示しているだけ・二重投稿でもコピーでもない・mypace でも見られます」。
- → mypace の正式リリースを待たず独立公開可。むしろ hanoba が mypace を賑わす燃料になる。

## 3. 実装方針

- 既存実績のある **Astro ＋ React islands ＋ PWA（`@vite-pwa/astro`）＋ Cloudflare Pages ＋ Tailwind** 構成を骨格として踏襲する（独自バックエンド／DB 層は持たない）。
- **mypace の既存コンポーザー（アップロード／クロップ／レトロ加工）を流用**する。React 同士なので島として移植する。
  - クロップ → **正方形ロック＋ドラッグで位置決め**に変更。**角度回転（#314・mypace ImageEditor 方式）**: `CropFrame` 下のコントロール＝**[左90°][−0.5°][===微調整スライダ===][+0.5°][右90°]**（90度ボタン＝向き直し／スライダ＋0.5刻みボタン＝水平出し・±15°）。**プレビューは即時の CSS `transform: rotate()`**（blob 再生成の遅延なし＝kako-jun「遅れて回転する／何も起きない」を解消）。クロップ枠は元画像の box（軸整列）に引かれ、**焼き込みは `renderInPlaceRotation`**（mypace 同方式＝元画像と同寸の canvas に中心回転で描く・crop 矩形は元画像の自然座標のまま一致）で**プレビューと同じ見え**を再現する。**90/270 回転後のクロップ枠制限（#348）**: in-place 回転だと写真は中心の `min(W,H)` 正方形だけが見え左右（縦長は上下）は letterbox の空き帯になるが、react-image-crop の操作領域は回転前 box のままなので枠を空き帯（写真外）へ出せてしまう。純関数 `clampCropToVisible(crop, rotation, boxW, boxH)`（テスト済み）で**見えている中心正方形に clamp**する（ドラッグ中=`onChange`／90度跨ぎ=`quarter` 変化の effect で収め直し＋commit）。0/180・微調整回転は素通し（退行なし）、焼き込みは座標系不変なので**プレビューと一致**を保つ。crop は座標系が変わらないので**リセットしない**（v1 のリセットが招いた「未設定」表示も解消）。rotation は draft に永続化。純粋部 `rotationFine`（微調整成分）はテスト。在りし v1（回転済み blob を src 差し替え）は遅延と未設定の原因だったので撤回。**撮影日（#324）**: 添付時に各写真の EXIF `DateTimeOriginal`／ファイル名から撮影日（`YYYY-MM-DD`）を検出し（`src/lib/image/exif.ts`・純関数）、コンポーザの「撮影日」欄で**編集・除外**できる（不正確な EXIF 対策）。投稿時に distinct を kind:1 の `["shot_date", 日付]` タグへ載せる（本文は汚さない・他クライアントは無視・`DraftImage.shotDate` は draft 永続化）。これで投稿日とズレても**撮影の草は撮影日で積まれる**（後日まとめ投稿でも撮った日数が点く）。
  - 植物写真向けの穏やかな補正 → ランダム適用でなく**選択式**。細かなスライダーは畑で使いにくいので増やさず、葉色や花色を潰さない控えめなプリセットにする。
    - **重ねがけ式プリセット**（#140/#155/#156）: 淡陽(明るさ)／翠露(S字トーン)／土香(逆S字トーン)／美華(彩度・彩度を上げるのは美華だけ)／影暮(周辺減光)／霞幻(周辺ぼかし)／線明(シャープ)。翠露＋土香 は相反するので同時選択で完全相殺。
    - **各効果は なし/弱/中/強 の4段**（#171）: プリセットは弱/中/強の3段（`levels`）を明示的に持ち、**中＝既定の効き**・線明（シャープ）は効きすぎたので中を控えめにする。チップは1回タップごとに なし→弱→中→強→なし で巡回し、**横長チップを3段階に塗りつぶして**現強度を見せる（色プリセットは持たず全部が効果トグル）。
    - **投稿時に canvas へ焼き込む**（プレビューと同じ見えを 1:1 出力に確定）。トーンカーブ(S字/逆S字)・周辺ぼかし(霞幻)・シャープ(線明)は CSS では表現しきれないため canvas のピクセル処理（LUT・blur 合成・畳み込み）で焼く。**プレビューは CSS（contrast/blur/減光オーバーレイ等）で近似**する＝畑で迷わない速さを優先し、最終の厳密さは焼き込み側に置く。実装は `src/lib/image/{presets,crop}.ts`。
    - **出力は長辺 1440px 上限に縮小（拡大はしない）・JPEG quality 0.85・フィルタは縮小後の小 canvas に焼く・複数枚は並列アップロード**（#167。nostr.build がどうせ縮小配信するため、巨大 JPEG の生成・CPU 処理・POST を短縮する。1:1 出力は不変）。
  - **下書きの自動保存・復元（#228）**: 書きかけのひとこと・写真・クロップ枠・フィルタ・並び順・選択中の写真を **IndexedDB に黙って自動保存し、開いたら黙って復元**する（「バックアップ／復旧」概念はユーザーに見せない・バナーも通知も出さない）。アップロード下書きは不変 Def でなく**状態側**なので `src/lib/composer/draft.ts`（SSR 安全・ランタイム依存ゼロのネイティブ IndexedDB 薄ラッパ）に分離し、写真 blob とメタ（本文/クロップ/フィルタ/並び/選択 id）を別ストアに持つ。本文が消えるのは ①本文右端の × ②自分で本文を空にする ③投稿成功 の3経路だけ＝**写真を選び直しても本文は保持**する（写真操作は本文に触れない）。
  - **写真の並べ替え（#274）**: 添付した複数写真（2枚以上）は、選択中の写真を **◀▶ で左/右へ1つずつ動かして順番を変えられる**（先頭＝カバー）。タップ＝編集選択は従来どおり。crop/filters は各写真（`DraftImage`）に内包されるので並べ替えは**配列入れ替えだけ**＝加工も写真と一緒に動く。並べ替えは純関数 `src/lib/composer/reorder.ts`（`moveById(items, id, delta)`・範囲外/不在は no-op・非破壊）に切り出し、Composer は `setImages` 経由で適用＝下書きの blobs(`order`)/meta(順序) effect が発火し並べ替え後の順序も自動保存・復元される。サムネ列直下に並べ替え行（`N枚目 / 全M枚`・端でボタン無効）。**◀ と ▶ はタップしやすいよう両端に離す**（中央にカウンタ・各ボタン 44px 標的）。
  - **画像編集の1手アンドゥ（#363/#393/#403/#406）**: 投稿前に、直前の画像編集を1手戻すボタンを**編集中写真（`CropFrame`）の右下隅にオーバーレイ**で置く（#406・撮影日=左下・枚数=右上のバッジと同じ流儀＝写真上の半透明ピル。`CropFrame` の `photoOverlayBottomRight` slot に渡す。親 slot は `pointer-events-none`、ボタンに `pointer-events-auto` でクロップのドラッグ操作と競合させない）。対象は kako-jun 指定の **角度（rotation）・フィルタ（filters）・Exif撮影日（shotDate/shotDateAuto）** と、**ユーザードラッグ由来のクロップ（crop）**（#393）。**クロップは自動センタークロップ・90度clamp 由来の commit は対象外**（画像ロード時の初期 commit や回転の副作用で履歴が汚れる/勝手にアンドゥが有効化するのを防ぐ。回転自体は別途 `onRotate` がアンドゥ対象）。**本文（一言）・タグは対象外**（本文はブラウザ標準のテキスト undo に委ねる）。配線は `CropFrame` の `onCropComplete(crop, fromUser)` で、ユーザーの `ReactCrop` `onComplete` だけ `fromUser=true`、`onImageLoad`・quarter clamp effect は `fromUser=false`。実装は `Composer` の `updateCurrentImage(patch, { userCrop, continuousRotation })` で変更前の `images` を履歴に積む ephemeral スタック（下書き永続化はしない）。**畳み込みは連続入力＝回転微調整スライダの1ドラッグだけを1手に潰す（ジェスチャ終端でリセット）**（#403。判定は**フィールド名でなく明示フラグ `continuousRotation`** で行う＝`CropFrame` のスライダ `onChange` だけ `setFine(v, true)` で連続を立て、`lastEditTagRef`＝畳んだ run の最初の状態だけ残す。さらにスライダの `onPointerUp`/`onTouchEnd`/`onMouseUp`/`onKeyUp` で `onRotateGestureEnd`＝`lastEditTagRef=null` を呼び、**1ドラッグ終端で畳み込みをリセット**するので次のドラッグは別の1手になる）。**離散編集＝90°・±0.5 ボタン・フィルタ・クロップ・撮影日は1アクション=1手＝毎回 snapshot を積み畳まない**（#403。旧挙動は「rotation 単独」というフィールド名で連続判定していたため 90°/±0.5 ボタンの離散クリックまで連続扱いで畳み、複数回の回転が1手にまとまっていた＝kako-jun の不満を直す。フィルタも on→on→off と続けて変えると、従来は同一 field 連続変更として畳み唯一の snapshot が「最初=空」になり off→undo で空のまま無変化だった実機バグを直す＝離散を畳まないことで off 直前の snapshot を積み戻せる）。**crop は `cropSyncToken` で undo 時に表示を再同期する**（#403。`CropFrame` は crop を内部 state＝uncontrolled で持ち `initialCrop` は `onImageLoad` の初期値専用なので、id 不変のままの undo は再マウントされず矩形が動かない。`undoLastEdit` が `setCropSyncToken((n)=>n+1)` で increment し、`CropFrame` の effect が外部復元値＋`imgRef` の自然寸法から `squareRectToPercentCrop`（`onImageLoad` と共通の純ヘルパ）で % crop を引き直して `setCrop` し直す。初回 token=0・未ロード時はスキップ＝`onImageLoad` が初期化を担う）。画像の追加/削除/並べ替えなど構造変更が入ったら履歴はクリア（古い blob 参照を復元しない）。履歴が空ならボタンは不活性。
- **一言の `#` ハッシュタグ補完**: 入力中の `#` を**過去に使われたタグから補完**し、同じ植物のタグへ投稿が自然に集積する（emergent taxonomy＝固定の分類表を持たずタグが育つ）。補完候補の源はリレー上の過去 `t` タグ。よく使う種は Wikipedia 由来で seed 可、細かい品種名（cultivar）は Wikipedia に無いので freeform `#`（過去使用のみで補完。エンティティ紐付け `@@` の UX を似せるだけ）。既存のメンション補完 UI を流用。
- 設計のトーンは mypace に揃える（スキンであるため）。
- **PWA インストール促し（#230）**: `beforeinstallprompt` を捕捉して `preventDefault()` し event を握り、控えめなバナー「ホーム画面に追加」＋[追加]/[あとで]で `prompt()` を呼ぶ。既設置（`display-mode: standalone` / iOS `navigator.standalone`）なら出さない。iOS Safari は `beforeinstallprompt` 非対応なので未設置時だけ手動手順ヒント（共有→ホーム画面に追加）を出す。却下を localStorage に記憶し 7 日抑制する（思想は mypace 由来）。バナー先頭には**実アプリアイコン（ハノーバ市旗の H・`public/icon.svg`）を `<img>` で出す**＝ホーム画面に追加される当のアイコンを見せて「何を追加するのか」を分からせる（汎用の芽アイコンの仮置きは廃止）。状態（却下記憶・抑制判定の純関数）は `src/lib/pwa/install.ts`、表示は島 `src/components/pwa/InstallPrompt.tsx`（MainLayout に `client:idle`・window/localStorage はマウント後参照で SSR 安全）。
- **i18n 基盤（#147 / #384）**: zero-dep の hand-rolled i18n（ライブラリ依存を足さない＝薄いまま）。`src/lib/i18n/`＝`locale.ts`（`Locale="ja"|"en"|"zh"|"es"`・`DEFAULT_LOCALE="en"`・`isLocale`・**#384 で zh/es 追加・並びは reach 上位 `[ja,en,zh,es]`＝zh→es**）／`messages/ja.ts`（フラット dot-key カタログ＝**文言と型 `MessageKey` の単一ソース**・`as const`）／`messages/en.ts`・`zh.ts`・`es.ts`（各 `Partial<Record<MessageKey,string>>`＝**完備義務を課さない**・未訳は ja に fallback）／`t.ts`（`t(locale,key,params?)`＝`{name}` 補間＋ja fallback）／`useT.ts`（島用フック）。**言語追加は `LOCALES` に足し `messages/<lang>.ts` を作るだけ**（殻 swap・市民ラベル・`LangSwitcher` は N 言語化済み・#419/#420 で es/zh 実証）。**文言を増やすときは ja.ts にキーを足す**（ハードコード禁止＝型でキー漏れを検出）。サーバ（.astro）は `t(locale,key)`、島は `lang` prop＋`useT(lang)`（MainLayout がページの locale を島へ流す）＝**文言経路を単一に**保つ。世界観文言は直訳でなく意訳（「育てて、見せる。」→ "Grow it. Show it."）、学名 `sci` を言語横断の共通キーにする。
  - **方針確定（Twitter 型・en 基本・kako-jun session692）**: hanoba の実コンテンツ（投稿）は FeedGrid が**クライアントで Nostr から描画**＝静的 HTML に載らないので、言語別ルーティング（`/en/` `/ja/`・hreflang・言語別sitemap）で得る SEO は薄い殻（ヒーロー/about）だけで**ごく小さい**。hanoba は「翻訳マーケサイト」でなく **Twitter 型の SNS**（UGC は単一言語のまま・大手 SNS も言語別 URL を切らない）。よって:
    - **同一URL・言語別ルーティング無し・hreflang/言語別sitemap 無し**（将来 about/宣伝ページだけ各言語で上位を取りたくなったら、その数ページにだけ足す）。
    - **en 基本**＝世界の正面。go-live 時に `DEFAULT_LOCALE="en"` で**en を静的ビルド**（殻の英語が素の HTML＝海外 SEO はこれで十分）。
    - 言語は **navigator.language 検出＋localStorage 上書き＋切替UI**。日本語ブラウザは**描画前 inline swap で ja に差し替えてから paint**（市民手帳ラベル #262 の flash-free 手法＝`data-i18n`＋localStorage を first paint 前に読む、を殻の文言に一般化）。殻は薄いので inline 重量も軽い。Twitter が動的サーバで言語出し分けする所を、静的 hanoba は inline pre-swap で代替する。
    - **投稿SEO（#542）**: 通常フィードは引き続き Nostr クライアント取得だが、build 時に取得できた最新投稿だけ `/p/<nevent>` の静的ページを生成する。各ページは JS なしで caption / 画像 / canonical / OGP / Twitter Card / JSON-LD を持ち、sitemap に載る。Nostr 取得失敗・0件なら固定ページのみでビルド継続する。
    - **表示既定は en／フォールバックの安全網は ja**（型ソース＝常に完備な `ja.ts`・kako-jun の原文）。en に穴があっても生キーを出さず ja を見せる。
  - **段階2（en 基本化）の順序**: ①全文言をカタログへ**抽出**（言語中立・ja のまま byte-identical＝本番は壊れない）→ ②en 翻訳を埋める → ③**go-live PR で `DEFAULT_LOCALE`→en＋inline swap＋切替UI**を一括投入（**ここは見た目が変わる＝ライブ blink サインオフ**）。半端な「殻だけ英語・本文ja」を本番に晒さないため、③は en カバレッジが揃ってから。段階1（PR #334）はランタイム locale を `DEFAULT_LOCALE`(当時 ja) 固定＝**挙動不変**で基盤と1スライス（ヘッダ／ホームヒーロー／投稿FAB）だけ移管済み。**品種学名 `sci` は #200 で実装済み・英名 `en` 投入は段階3。hreflang/ルーティングの段階4 は上記方針で原則不要に縮退。**
    - **現況（#384/#385）**: ja/en に加え **zh/es を同一URLのまま追加**（4言語・#419/#420）。殻 swap は単一 swapLocale でなく**非既定ロケール毎マップ**へ一般化、`LangSwitcher` は `LOCALES` 順の N 択。per-locale ルートは**作らない**（上記 Twitter 型方針の再確認・session705 で確定）。外国語投稿の caption は **PostDetail でブラウザ内蔵 Translator API によるオンデマンド翻訳**（#385・`translate.ts`＋`useCaptionTranslation`・表示層のみ＝Nostr本体/タグ/札不変・on-device 無料・非対応ブラウザはボタン非表示で原文のまま）。

## 6. データ（Nostr）— 検証済み契約

mypace と同じ Nostr 空間・同じタグ規約に準拠する（独自化しない＝mypace から見える要件）。

- **イベント**: `kind: 1`（テキストノート）。
- **タグ**:
  - `['t','mypace']` … **必須・自動付与**。これで投稿は通常の mypace 投稿と同一に mypace タイムラインへ出現。
  - `['t','hanoba']` … **必須・自動付与**。hanoba のフィードはこのタグで絞る（mypace の一般投稿を hanoba に流さないため）。
  - `['t','plantstr']` … **自動付与（#383・mypace 完全一致からの意図的逸脱）**。Nostr 全体の植物界隈（`#plantstr`）へ hanoba の投稿も露出させ、discover が `#plantstr` を「読む」のに「書かない」非対称を解消する（reciprocity・生態系への還元）。**kill switch**＝`buildAutoTags()` のこの1行を消して再デプロイすれば全ユーザー分が止まる（可逆）。**これはコミュニティ・ハッシュタグの自動付与であって、本文 `#` を `t` タグ化するルールではない**（下記の「本文 `#` は `t` 化しない」とは別軸）。
  - `['client','hanoba']` … 自動付与。可視性には影響しない。
  - **植物トピック・種別は、一言（本文）にユーザーが書く `#ハッシュタグ`** で表現（例: `#アガベ #パキポ`）。本文に見えるテキストとして残す。**hanoba 側に固定の植物 taxonomy は持たない**。**本文 `#` は `t` タグ化しない**（mypace と完全一致＝独自化禁止。確定）。集約は書き込み側のタグでなく、読み取り側の二段構え検索で行う（下記）。
- **content**: 必須の一言 ＋ 画像 URL（1〜4個）を**インラインのプレーン URL**で（`https://image.nostr.build/<hash>.<ext>`。imeta タグは使わない）。**一言が空（URL のみ）は弾く**。**本文 caption 末尾に `#plantstr` も自動併記する（#408・#383 follow-up）**＝`t:plantstr` タグに加え本文文字列としても `#plantstr` を残し、① NIP-50 全文検索（本文の "#plantstr" を引く層）／② 本文 `#` を parse する他クライアント の双方へ hanoba 投稿を届かせ reach を最大化する（`buildNoteTemplate` の content 合成・画像 URL より前＝caption 側）。**重複ガード**＝caption に既に `#plantstr`（大小無視）があれば足さない（手書きを二重化しない・`extractHashtags` で判定）。**kill switch**＝`buildNoteTemplate` のこの併記数行を消せば止まる（可逆）。これは `t:plantstr` 自動付与（コミュニティ・ハッシュタグ）の本文側補完であって、**品種など本文 `#` を `t` 化するルールではない**（その別軸は不変）。
- **画像アップロード**: `POST https://nostr.build/api/v2/upload/files`（NIP-98 認証）。EXIF はサーバ側で自動削除。
- **リレー**: `wss://relay.damus.io`, `wss://nos.lol`（必要時に検索 `wss://search.nos.today`）。
- **鍵**: ブラウザ生成・localStorage 保存、NIP-07 任意。
- 既存コンポーザーの流用方針は §3（クロップ→1:1 ロック、レトロ加工→選択式）。
- **タグの集約（読み取り）＝二段構え**（mypace 準拠・#3/#4 で実装）: ① `{"#t":[tag]}` フィルタで `t` タグ持ちを取得、② NIP-50 `search:"#tag"` で**本文ハッシュタグ**を全文検索（他クライアントの `t` タグ無し投稿も拾う）、③ event id でマージ・重複除去。これが emergent taxonomy の本体で、書き込み側で本文 `#` を `t` 化しなくても集約できる根拠（書き込みタグは `t:mypace` / `t:hanoba` / `t:plantstr` / `client:hanoba`＝うち `t:plantstr` は #383 の reciprocity 自動付与で、本文 `#` の `t` 化ではない）。検索 relay = `wss://search.nos.today`。
- **discover の品種絞り込み（#239・実機 blink で多軸を全廃して再設計）**: discover は **「品種で絞るだけ」**。投稿者・期間・並び・共有・保存・本文キーワード検索・検索ボックス/探すボタンは全部廃止（kako-jun 指摘「絞り込みというより詳細検索だろ／矛盾だらけ」を受け、要らない軸を断つ）。状態は `tags: string[]`（品種＋自由タグ・AND）だけ。
  - **入力＝投稿画面と同じ `TagPicker`（filter モード）をそのまま再利用**（`VarietyFilter` でラップ）。長い品種名は打てないのでカテゴリ→属→品種のドリルダウン＋インクリメンタル検索で**選ぶ**。カタログに無い短い語は検索欄の「そのまま #◯◯ を使う」で**自由タグ**として足せる。選んだタグは AND のチップ（× で外す）。**チップの #タグ表示は閲覧言語に置換する**（カテゴリ→`Caudex Plants`・pickable 属→学名＝#460 と同じ境界。品種・技法・自由タグは ja 据え置き。`useFudaIndex` の `hashtagLoc` を `localizeHashtag` に通すだけ・#464）が、**`key`・`remove`・`caption` の実体は ja 正準のまま不変**＝cross-language フィルタ・選択色判定を壊さない。catalog/索引は `PostGrid` と同じ `useFudaIndex` フックで読む（投稿カードのタグ置換と同一経路）。filter モード（`TagPicker mode="filter"`）は投稿専用セクション（最近/人気/世話・記録/リクエスト）を出さず、品種を選んでも **#属を前置しない（葉タグのみ）**＝AND で過剰に絞らない（属タグの無い投稿を落とさない）。選択状態は選んだタグを `#a #b` の合成 caption で TagPicker に渡して判定（既存ロジックを変えない）。
  - **取得＝`fetchDiscoverFiltered`（tags のみの filter で呼ぶ）**: tags は別名展開した `#t` の OR（#23・「パキポ」→グラキリス等）＋ NIP-50 `"#tag"`、`applyClientFilter` で軸間 AND・画像のみを確定、**新着順**（並び替えなし）。tags 空なら既定母集団 **#plantstr ∪ t:hanoba**。選んだ／外した**その場で再取得**（検索ボタンなし＝ライブ・`latestRef` で stale 破棄）。読み取り側のみで §6 契約（本文 `#` は `t` 化しない）は不変。
  - **URL = `?tags=` の deep-link**: `applyFilterToParams`/`parseFilter`（旧 `?q=`/`?tag=` は後方互換で吸収）。意図的操作は `pushState`、復元（マウント/popstate）は `replaceState`/書き込み無し（ループ防止）。**popstate は絞り込みタグが実際に変わった時だけ再取得する（#427・`sameTagSet`）**: 投稿モーダルの `?p=`（#386）を閉じると `history.back` で popstate が出るが `?tags=` は不変なので、無条件に再取得すると `status:"loading"` でグリッドが一旦アンマウント→再マウントされ**スクロール位置が先頭へ飛ぶ**。`appliedTagsRef`（直近適用タグ）と URL のタグを `sameTagSet`（順序・大小無視の集合比較）で照合し、同じなら再取得をスキップする（モーダル開閉でスクロールを保つ）。マウントの初回取得は従来どおり常に行う。
  - **SW 注意（#291・#538・実機で踏んだ罠）**: PWA の Service Worker（workbox `generateSW`）は既定の `ignoreURLParametersMatching`（`utm_/fbclid` のみ）だと **`?tags=` 付きナビゲーションを precache の `/discover/index.html` にマッチさせられず**、ホーム fallback にすり替える＝**札クリックで discover が一切出ず、ホームが描画される**（dev は SW 無しで露見しない＝必ず build→preview か本番でSW有効下を検証）。`src/lib/pwa/workbox-options.mjs` の `ignoreURLParametersMatching: [/.*/]` で全クエリを無視させ、クエリ付き deep-link を各ページ HTML にマッチさせる。`?q=`（JSON-LD 検索）・旧 `?tag=`・`?p=<nevent>`（#386 投稿 deep-link）も同じ `[/.*/]` で同時に救済済み（追加設定不要）。同時に Hanoba は Astro の静的複数ページで SPA ではないため、`navigateFallback: null` で Workbox の全ナビゲーション→`/` fallback を無効化する。`/about`（市民手帳）・`/gazette`（市政だより）のような `client:only` island 中心のページも、SW は各ルート自身の HTML（または network）へ解決させ、トップ HTML に書き換えない。
  - **廃止の経緯**: 投稿者 `@名前` 解決が NIP-50 のハイフン演算子（search.nos.today は `kako-jun`→0件・`kako`→多数）で当てにならず需要も薄い／期間カレンダーは「使いたいニーズなし」かつネイティブ `<input type=date>` の区切り `/`・フォントが揃わない／並びは新着順だけでよい、として軸ごと撤去。`discoverFilter.ts`/`client.ts` に残る多軸の内部実装（author/keyword/since/until/sort・popular）は未使用化＝別 PR で掃除予定（#239 follow-up）。
- **タグ入力＝多段ドリルダウン＋検索（#22/#143/#144・実装済）**: 投稿時のタグは手打ちせず `TagPicker` から選ぶ。約 2,062 件の品種辞書を **カテゴリ→属→品種**（23 カテゴリ・近いもの順）の階層で持ち（`variety-catalog.ts`。**`TagPicker` を開いた時に動的 import で code-split**＝初期 composer バンドル非搭載・PWA precache でオフライン可。`variety-search.ts` は `catalog` を引数で受ける純関数でこの分割を保つ）、ドリルダウン（パンくず＋‹戻る・囲み外クリックで閉じる・入口は最上段）と全件横断のインクリメンタル検索（`foldForSearch`＝かな/カナ・大小・全半角を無視）で少クリック選択する。**タグと札を分ける（#181・#166 のタグ部分を撤回・#312 でカテゴリも反転）**: タグ＝Twitter式ハッシュタグ＝**概要→詳細の全階層**を付けてよい。品種を選ぶと**`#カテゴリ #属 #品種`**を本文へ入れる（カテゴリ→属→品種の順に `insertTag`・二重挿入防止で重複しない。非 pickable 見出し属配下の品種は `#カテゴリ #品種`）。**階層化は選んだ経路の文脈で行う**（#315・`tagsToPickAt`＝ドリルダウンの `cat`/`genus`・検索ヒットの `category`/`genus`/`genusPickable` を渡す）。これで**同名品種がカテゴリ跨ぎで存在しても経路どおりのカテゴリ/属**が入る（例 バラの「アイスバーグ」→`#バラ #アイスバーグ`／多肉の同名→`#多肉植物 #エケベリア #アイスバーグ`）。文脈の無い経路（freeform/人気/最近）は `tagsToPick` の名前先勝ち解決にフォールバック。解除（`tagsToUnpick`）も**本文の親タグで所在を曖昧さ回避**する（`findVarietyGenusInCaption`・無関係な別属を消そうとしない）。**タグは常に本文末尾へ追記し、散文とタグ群は空行（1行）で分ける**（#282＝`本文\n\n#tag …`。`insertTag` はキャレットに割り込まず末尾に積み、2個目以降のタグは同じタグ行へスペース区切りで継続＝空行は散文とタグ群の間だけ。`removeTag` で最後のタグを外すと空行ごと畳んで散文だけに戻す）。挿入後はキャレットを本文末尾へ送ってタグが見えるようにする（`focusEndSignal`・#165）。属の「このまま使う」は `#カテゴリ #属`。**カテゴリのクリックは常に品種選択へドリルインする（#364・#312 の『カテゴリ単独タグ』を撤回・kako-jun「カテゴリのクリックは常に品種選択と同じ動き／カテゴリ単体タグを打ちたい場合はない」）**: 検索ヒット・最近・人気でカテゴリをタップしても **`#カテゴリ` 単独は入れず、その階層へドリルイン**する（`engage` と検索カテゴリヒットとも `setCat` で入る）。属一覧画面の「#カテゴリ をこのまま使う」ボタンは撤去。カテゴリは品種/属を選んだ時の**前置タグ**（`#カテゴリ #属 #品種`）としてだけ付く（`tagsToPick`/`tagsToPickAt`・前置ロジックは不変）。属の「#属 をこのまま使う」（属＝具体的な植物識別なので残す）。filter は元からカテゴリヒット非表示・葉で絞るので影響なし。**札はカテゴリを出さない**（`resolveFuda` がカテゴリ label を札化しない＝札・ランキング無回帰）。**品種チップには学名(`sci`)を小さく薄くイタリックで併記**（#200・投稿前に学名を確認でき #23「学名を厳密に書きたい層」に応える＋同名異植物の見分け。`SciName`・`aria-hidden`＝ボタンのアクセシブル名は `#品種名` のまま保つ。属/カテゴリ/世話記録タグには出さない・sci 無しのグループ概念は和名のみ）。札（鉢の名前＝具体1つ＝学名＋和名）はタグとは別概念（#182・`fuda.ts`＝`buildFuda` でハッシュタグから属＋品種を組む。`detectPlants`/`detect.ts` は撤去・`spec.md` の §4 投稿詳細を参照）。集約は読み取り側の OR 検索（§6・別名横断）でも吸収できるが、属タグを直接付けることで属でも確実に辿れる。人気/最近で**属をタップしたら階層へ誘導**。選択済みタグは満たし色（緑）になり、**再タップで解除**（`tagsToUnpick`＝品種を外すと同属に兄弟が残らなければ属、カテゴリに他属が残らなければカテゴリも連動撤去。属を外したときも同カテゴリに他属が残らなければカテゴリを連動撤去＝#312）。最近使ったは**投稿成功後**に本文のタグを記録（タップしただけは残さない）。投稿の種類/トラブル/世話/記録/特徴/仕立てのクイックタグ（`tag-catalog.ts`）は別枠で常設（投稿の種類＝**成長記録/質問/実験**〔#311・「投稿が何か」＝投稿タイプ・先頭の枠。成長記録/実験は植物の出来事〔開花/結実〕でなく投稿タイプなので「記録」から移した・kako-jun〕、トラブル＝**失敗/病気/虫害**・特徴＝斑入り/綴化/石化/変異・仕立て＝水耕/板付け 等は品種と別軸の投稿タイプ/困りごと/自慢/栽培形態タグ・#221。斑の細分＝錦/黄斑 等は「斑入り」へ一本化・#251。分類語でないので #166 非抵触）。**入る値は本文 `#` テキストのみ＝`t` 化しない**（§6 契約不変）。旧 `PlantSuggest`/`childrenOf`（#63/#23 Phase2 の俗称検出・「もっと具体的に」）はこのドリルダウン＋検索に置換し撤去。クイックタグ（世話/記録/特徴/仕立て）は**カテゴリ内を作業順/時系列に並べ**（#251 で頻度順から変更）、先頭 `INLINE_LIMIT` 件を常時インライン表示する。あふれが `MIN_OVERFLOW` 件以上ある行だけ行末「その他」で残り全件を見せ、それ未満の行は全件インライン（＝**世話/記録だけ「その他」が付き、特徴/仕立ては全インライン**＝『下のカテゴリにその他が無い／開いても数件』のガタつきを解消・#251）。ポップアップは**画面中央の不透明モーダル**（portal＋scrim＋glass-strong・×/Esc/scrim で閉じる・#243＝旧・トリガー半重ねの absolute は廃止）で**あふれ分だけ**見せ、インライン済みの定番は重複させない（#186）。発芽は世話へ移し 発芽→発根→実生 を隣接、記録から発芽を削除（#169）。**タグに載せる/載せない の2原則（#251・kako-jun 実機レビュー・#311 で原則①を細分）**: ①**症状の細目タグは不可／大分類のトラブルタグは可**＝徒長/葉焼け/根腐れ/うどんこ病/カイガラムシ 等の細目は本文に書く（粒が細かすぎ集計を壊す）が、`失敗`/`病気`/`虫害` は**トラブル**として載せる（将来「トラブルだけのTL」の余地）。`質問` はトラブルとは限らないので投稿の種類に残す／②現実の家・私生活を指すタグ禁止（ハノーバ＝架空都市の世界観内＝『我が家の植物』を排除）。写真追加「＋」のポップアップ（ImagePicker compact）も同様にトリガーに半重ねで出す（#187）。タグ（品種）が無い植物は「追加をリクエスト」導線から **`/vote` の「品種への要望」板**（住民投票 BBS の先頭・Nostalgic）へ送る（#232・新規タブで開き合成中の下書きを保つ）。**旧 GitHub Issue 起票は廃止**＝一般ユーザーを GitHub に飛ばさない。品種に関する要望（並び順・追加・その他）はこの板に集約する。**植物タグの canonical＝catalog の正準名＋`aliases`（#409・F1 パイロット実装）**: 1品種=1エントリ（一意の正準 name）とし、表記揺れ・同義語は同一エントリの `aliases` に集約する（**読み=別名→正準へ解決**＝`buildFuda`/`findVarietyGenus` が `[v.name, ...aliases]` で照合・**書き=正準名で収束**＝検索/選択で別名に当たっても本文に書かれるのは正準 name〔`searchCatalog` の hit は `v.name`／`tagsToPick` は一致品種の `variety.name` を返す〕）。同一植物の表記揺れ/同義語は1エントリ＋`aliases` に統合する（例 ベイチー＋alias ヴェイチー〔Platycerium veitchii〕・デショウジョウ＋alias 出猩々・パクチー＋alias コリアンダー）。**`sci` はキーにしない**（別品種が裸の属 `sci` を共有する・cultivar 表記が割れるため）。**別属の同名は統合しない**（属文脈で別管理＝#315・例 ベイチー〔Platycerium〕と ベイチー〔Anthurium〕は別エントリのまま、属共起で曖昧解決する）。**「盆栽」プラントカテゴリは解体済み（#414）**＝盆栽は品種でなく仕立て（tag-catalog の仕立て軸へ）。松柏類（黒松/真柏…）と園芸コニファーは**針葉樹だが用途＝庭木**なので**新カテゴリを作らず「花木・庭木」へ一本化**（コニファー以外の針葉樹が来ても特別扱いしない・aliases に「針葉樹」で1検索集約・kako-jun）。**P-canonical 一巡完了（#417/#418）**＝同sci・同カテゴリの表記揺れ **48組を正準名+aliases に統合**（多肉/塊根 19・観賞/食用 29）、**35組は distinct で温存**（バラ分類・菊の仕立て・米の果皮色・トマトのサイズ別 等＝over-merge 罠を回避）。**正準名は「江戸っぽい吉祥的な和名」を札の代表に出さない（kako-jun の好み）**＝恵比寿/吉祥 等の縁起物の和名より**学名カナ**が趣味家の通用名なら、正準を学名カナにして縁起名を `aliases` へ畳む（例 ブレビカウレ正準・恵比寿笑い alias／**ナマクアナム正準・光堂 alias〔#461〕**。Pachypodium はもともと グラキリス等 学名カナ正準が主で 恵比寿笑い・光堂 が例外だった＝この2つを寄せて属の一貫性が揃う）。**書き=正準（学名カナ）に収束するが、既存の `#恵比寿笑い`・`#光堂` 投稿も alias 解決で同じ札/フィルタに当たる**（read=別名→正準）。一律 sweep でなく**個別判断**（笹の雪・朧月 等、和名が通用名のものは温存）。**寄せられる条件＝「学名カナが趣味家の実通用名であること」（#461 で精査）**。これを満たさない縁起名は temochi（温存）する: ①**寄せ先が構造的に無い**＝学名の品種名そのものが縁起名（Agave 'Kisshokan'=吉祥冠、Vanda falcata 'Kenkokuden/Fukiden/Houmeiden'=建国殿/富貴殿/豊明殿）、②**学名カナが通用名でない**＝吉祥天/恵比寿大黒・メセン玉名（福来玉/招福玉/富貴玉/国宝玉）は学名カナ（トルンカータ/フルレリ 等）を趣味家が使わず、**帝玉と同じ立ち位置の定着玉名**。無理に未知のカナへ置換すると「通用名は温存」を自分で破るため、これらは縁起名でも canonical のまま残す。**P2 多言語＝カテゴリの閲覧言語化（#409・#384 一体・PR1 実装済）**: 品種名の大半は固有名詞カルティバ（白鯨=Hakugei・赤猫=Red Catweezle＝訳でなく綴り替え）で全多言語化は価値が薄いため、**効果の芯＝非日本語ユーザーが TagPicker のナビ入口を辿れるよう「カテゴリ→属」を訳す**ことに絞る（カルティバ品種は固有名詞のまま）。`VarietyCategory`/`Genus`/`Variety` に `loc?: Partial<Record<"en"|"zh"|"es", string>>` を持たせ（ja base = `label`/`name`）、**表示だけ**閲覧言語化する純関数 `pickLoc(base, loc, locale)`（`plant-i18n.ts`＝ja は常に base・非対応言語名は base へ graceful・`categoryLabel`/`genusLabel`/`varietyLabel` ラッパ）を `TagPicker`（一覧/パンくず/検索ヒット/文脈）に当てる。**書き込むタグ・内部キー・`tagsToPick`/`tagsToPickAt` の戻り値は locale に依らず常に ja 正準のまま**＝誰がどの言語で見ても同じ canonical（日本語）タグを書く（cross-language filter の核・独自化禁止）。検索（`foldForSearch`/`searchCatalog`）と言語横断 alias（`buildCatalogAliasIndex`・discover の `resolveTagAliases` 合成）にカテゴリ `loc` 値を合流＝閲覧言語のカテゴリ名（"Succulents"/"Suculentas"/"女仙"）でフィルタすると `#多肉植物` 投稿に AND 到達する。**PR1＝カテゴリ23・PR2＝属222を populate 済み**（属に en/zh/es を入れた・品種は固有名詞カルティバなので ja のみで据え置き）。#474 でヤシ/ソテツ/メロン/スイカ/コールラビ/梨の6属を追補し loc 同梱、苔玉/苔テラリウムは品種でなく仕立て（tag-catalog の仕立て軸＝盆栽#414 と同じ）へ移設。#476 でスーパー定番の野菜21属（ねぎ/かぶ/ごぼう/れんこん/しょうが/アスパラ/セロリ/ニラ/山芋/チンゲン菜/菜の花/空心菜ほか中国野菜・しいたけ＝唯一の菌類例外）＋果樹のあんず/プルーン/ネクタリンを追補し loc 同梱（属249）。品種レベルの多言語 `aliases`（英名/学名の別表記横断）は引き続き #409 後続。
- **植物の別名 OR 検索（#23 Phase 2・実装済）**: タグ軸が辞書の植物に解決できる場合（`tagAliasValues`＝`findPlantByTerm`/`plantTagValues`）、その植物の**全表記を横断**する。多軸エンジン `fetchDiscoverFiltered`（#131）が `#t` を別名展開して OR 取得（`{"#t":[名前, 別名, 単語学名…]}`）し、`applyClientFilter` の軸間 AND もタグごとに**別名集合で照合**する（例: タグ「パキポ」が `#グラキリス` 投稿に一致）。PostDetail の植物チップはそのタグに絞り込み（`#<著名名>`→tags 軸）この経路に乗る。**別名集合は dictionary（#23）∪ `variety-catalog` の属/品種別名（#303・札と同じ source of truth）**＝辞書外の属別名でタグした cross-client 投稿（例 `#ゴムの木`＝フィカスの別名・dictionary に無いが catalog にある）にも AND が当たる（`buildCatalogAliasIndex`＝filtering 時に variety-catalog を動的 import して索引を1回構築・キャッシュ、`tagAliasValues` と合成。失敗時は dictionary だけで動くグレースフル）。hanoba native 投稿は canonical 属名を書くので元から往復し、本件は cross-client/手打ち非 canonical の recall 改善。
- **花リアクション＝NIP-25 リアクション＋トグル取り消し（#529/#537）**: 投稿詳細の花ボタンから標準 `kind:7`、`content:"+"`、tags `["e", post.id]` / `["p", post.pubkey]` を publish する。テンプレートは純関数 `buildReactionTemplate`、自分の既存反応確認・署名・relay publish・`confirmEventStored` による読み取りリレーでの保存確認は `client.publishReaction` に集約する（独自の色・残高・複数加算タグは持たず、同一 pubkey の**最新反応**が既に有効な花なら再送しない）。**花を添えたかどうかは localStorage に控えない＝正本は常にリレー上の event**。投稿を開いた時／トグル操作の直後に `client.fetchReactionState(eventId)` が `{kinds:[7], "#e":[eventId]}` を1回取得し、`countLikes` で件数を、自分の pubkey の最新反応（`latestReaction`）が `isLike` ならその event id を `myReactionId` として返す（取得失敗はグレースフルに `{count:0, myReactionId:undefined}`）。花を添えた状態でボタンを押すと、その `myReactionId` を対象に NIP-09 `kind:5` を publish する（`client.deleteReaction`。`deleteComment` と同じ削除テンプレート `buildDeletionEvent` を再利用するが、件数表示に直結するため `publishReaction` と同様に `confirmEventStored` で保存確認を**必須**にし、確認できなければ throw して件数を不正に減らしたままにしない）。publish／削除いずれの操作も成功後は必ず `fetchReactionState` を再取得して件数・自分の状態を relay 由来の値に揃える（ローカルで ±1 演算しない）。最新は `created_at`、同秒はイベントidの辞書順で決定しrelay返却順に依存させない。1リレーの accept だけでは成功にせず、保存未確認時は失敗としてUIの件数・状態を変更しない。タイムライン/discover の `fetchEngagementCountsBatch` は同じ `{kinds:[7,1], "#e": ids}` の結果から count と `myReactionIds` を作り、カードCTAの塗り花/文言にも relay 由来の自分状態を渡す。詳細モーダル内で花・コメントが変わったら `PostGrid` へ件数と自分状態を戻し、モーダルを閉じた後のカード表示も即時に更新する。
- **コメント＝Nostr リプライ（#142・実装済）**: 投稿へのコメントを **NIP-10 のリプライ（`kind:1`）** として表現する（投稿詳細モーダルの `CommentSection`）。hanoba から Nostr に「足す」経路の一つ。
  - **タグ**: `["e", <親投稿id>, "", "root"]`（親投稿を root に印付ける direct reply。relay ヒントは空）＋ `["t","mypace"]` ＋ `["client","hanoba"]`。
  - **`p` タグ（@呼びかけ）は付けない**: コメントは投稿者を名指しで呼び出す通知でなく「その投稿にそっと添える一言」（静かな観賞 SNS の posture・確定）。
  - **`t:hanoba` は付けない**: hanoba フィード/人気タグ集計は `#t:hanoba` で絞って**画像投稿だけ**を集める（`fetchHanobaFeed` / `fetchPopularHashtags`）。文章だけのコメントに `t:hanoba` を付けるとフィードのノイズ・タグ集計の汚染になる。コメントは親の `e` タグ経由（`{kinds:[1], "#e":[postId]}`＝`fetchReplies`）で読むので不要。
  - **読み取り**: `{kinds:[1], "#e":[postId]}`（`fetchReplies`）。このフィルタは親を `e` タグで参照する kind:1 を**全部**返すので、引用リポスト（NIP-18・`["e", postId, "", "mention"]`）が混ざる。`toComments(events, postId)` は**本物のリプライだけを残す**＝「値が `postId` に一致し marker（`tag[3]`）が `"mention"` でない `e` タグ」を1つでも持つもの（`"root"`/`"reply"`/マーカー無し＝残す、`"mention"`＝引用リポストは落とす）。あわせてリレー間の重複は id で除去。表示は古い順/新しい順を切り替え（`sortComments`）。**削除**は投稿と同じく NIP-09 `kind:5`（`deleteComment`。コメントは画像を持たないので実体削除は無い）。
  - ビルダーは `buildReplyTemplate`（純関数・空 content / 空 parentId は throw）、relay 呼び出しは `client.ts`（`fetchReplies` / `publishReply` / `deleteComment`）に集約（§3）。本文 `#` を `t` 化しない §6 契約は不変。
  - **未名乗り表示（#531）**: 投稿カード・投稿詳細・コメント・公開プロフィールは `src/lib/feed/author.ts` の純関数を共有し、kind:0 の name を trim 後非空のときだけ名乗り済みとする。可視 fallback は市民レベル文言とは分離した `author.unnamed`（locale ごとの「旅人」相当）。Nostr 上の pubkey、プロフィール URL の npub、削除時の所有者比較は変えない。コメント著者も `/u?npub=…` へリンクし、短縮 npub は可視名ではなく、4経路のプロフィールリンク／見出しで同名利用者を区別する `aria-label` の補助情報に限る。
- **住民投票＝Nostalgic BBS 埋め込み（#160・`/vote`）**: 市民手帳の全ページ下部に共通表示する「市政の窓口」strip（#510 方針B）に置く、最初に開庁した役所。フッタにも同名リンクを常設し（#525）、手帳だけの孤立導線にしない。投票（意見集約）は **kako-jun 自作のバックエンドレス掲示板 Nostalgic BBS**（外部ホスト・`https://nostalgic.llll-ll.com`）を web component で埋める＝hanoba 側にサーバ/DB を足さない（`spec.md` の §7 non-goal 維持）。
  - **埋め込み規約**: web component `<nostalgic-bbs id="..." theme="dark" width="100%">` ＋ ローダー `https://nostalgic.llll-ll.com/components/bbs.js`。`theme="dark"` が暗色シック（`DESIGN.md` の §5）に馴染む。`/vote` は専用 1 ページなのでローダーは `<script is:inline src=... defer>` で **1 度だけ**読む（多ページ footer の冪等注入は不要）。Astro は外部 src をバンドルせず、component はクライアントで upgrade（板＋投稿フォームを描画）＝SSG を壊さない。板の実描画・dark テーマ・投稿動線は**実ブラウザでのみ確認できる**（jsdom/build では見えない）。
  - **3 板（公開 embed id）**: 品種への要望 `hanoba-16b82341`（**品種に関する要望は全部ここ**＝並び順・名簿に無い品種の追加・その他。TagPicker #169/#232 の「追加をリクエスト」もこの板へ集約＝GitHub 廃止。先頭＝一番上に置く）／欲しい機能 `hanoba-76cf0aed`（機能リクエスト）／バグ報告 `hanoba-12e8672f`（不具合報告）。各板は glass 列で囲み（`DESIGN.md` の §5.0 原則2）、見出し＝h2・節は `aria-label` 付き。
  - **幅ポリシー（#540）**: `/vote` のページ外枠と `SiteHeader` は、`/ranking`・`/about` と同じ広幅コンテナ（lg で `max-w-5xl`）に揃える。一方、Nostalgic BBS の各板は読み書きしやすい掲示板幅を保つため、各 BBS セクションを `max-w-3xl` に留め、`width="100%"` はその内側に追従させる。デスクトップで外枠ごと狭く見せないが、掲示板本文だけは横に伸ばしすぎない。
  - **開放範囲**: `/vote` は**全員に開放**（市民レベルのゲートなし）。ハブからは L1+ が辿るが、直接アクセス・投稿は無制限（名前は任意）。
