# Hanoba — DESIGN

> 植物写真の SNS。1投稿 ＝ 正方形写真1〜4枚 ＋ ひとこと（必須）。**「育てて、見せる。」**
> **mypace（Nostr）のスキン**であり、自前のバックエンドを持たない。

Instagram より早く・かんたんに、文章を添えて植物を見せる場。世界観は**暗くシックな大人のコレクター部屋**（§5.0）。畑・ビカクシダ・種まき・パキポ・アガベの自慢と、実験の逐次メモを兼ねる。長期目標は **架空の植物好き都市ハノーバ(Hanoba)をみんなで作る**こと。

---

## 1. 原則（規律）

1. **正方形固定** — 投稿画像は正方形に矯正。クロップはドラッグで位置決め。1投稿に1〜4枚まで入れられる。
2. **一言必須** — 写真だけの投稿を許さない。`content` 空は弾く。これが心臓部。
3. **動画非対応** — 身バレ（背景の映り込み・環境音・反射・複数フレーム）回避。静止画のみ。
4. **植物だけ** — テーマは固いが、その枠内は何でも OK（自慢・実験メモ）。

## 2. アーキテクチャ

- **自前バックエンドなし。** 状態は全部 Nostr に乗る。投稿は **mypace 経由でなく共有 Nostr 空間へ直接**行う。「mypace のスキン」の実体は **同じ Nostr タグ規約を使うこと**。
  - 補足（#28・実装確認）: mypace 自体は Workers＋D1 の API を持つが、**Hanoba はそれを使わない**。投稿（kind:1）・削除（kind:5）・プロフィール（kind:0）・画像アップロード/削除（nostr.build NIP-96）は**全てクライアント側**で完結し、バックエンド不要を維持する。
- **アカウント（#28）**: nsec はブラウザ生成・localStorage 保存（`getOrCreateSecretKey`）。**ユーザー名（kind:0）を入れたら投稿できる**（見るだけ→参加）。**既存ユーザーは nsec を持ち込める**（`AccountName` の「すでにアカウントをお持ちですか？」→ `importNsec` → 既存 kind:0 全体（名前＋picture/about/websites）を `fetchMyProfileResilient` で引き継いで控えにシードし reload）。取得は bounded retry で行う（単発だと接続直後に websites を載せた版を取りこぼし、控えに `websites:[]` を焼いてしまう＝#93）。mypace 等のユーザーがアカウントを無駄に増やさない。鍵は端末（localStorage）にのみ保存。
- **削除＝写真と一蓮托生（#28）**: 投稿削除は ①NIP-09 `kind:5` を publish（mypace/Nostr 上で不可視化）＋②**nostr.build の画像を本人の鍵で NIP-96 削除**（`DELETE /api/v2/nip96/upload/<sha256>`・NIP-98 認証）。「自分の植物」`/me` から行う。D1 を持つ mypace と同じ"消せる一芸"を backendless で再現（画像 URL は投稿本文に内包されるので upload 履歴 DB も不要）。鍵を失うと過去投稿は消せない＝鍵の重要性に注意。
- **編集＝確認つき再投稿（#300・mypace 由来）**: Nostr はイベント不変なので「編集」＝**新しい投稿を publish して旧イベントを NIP-09 `kind:5` で削除**する（`client.editPost`＝**新規 publish → 旧削除**の順。先に消さない＝publish 失敗で投稿が消える事故を防ぐ）。**写真は本文 URL を再利用する（再アップロードしない）**ので、`deletePost` と違い nostr.build の実体は消さない（消すと再投稿側の URL が死ぬ）。**いいね・コメントは旧イベント id に紐づくため引き継がれない**＝`/me`（`MyGrid`）の編集ボタン→`EditPost` モーダルで本文（画像 URL を除いた全文＝`#`タグも本文の一部として直す）を直し、保存時に**「いいね N・コメント M は引き継がれません」確認を1段挟んで**から実行する。写真の追加/削除/差し替えは現状なし（本文編集のみ・follow-up）。
- **市民の活動スタッツ（#272・段階1）**: `/me`（`MyGrid`）に**活動スタッツ節**（`CitizenStats`）を出す。**全て t:hanoba 投稿からのクライアント集計＝backendless・新たな身バレ無し**（公開投稿を数えるだけ・ステラ/閲覧数は不採用）。項目＝**投稿数・写真枚数（imageUrls 合計）・育てた品種数＋一覧・居住日数（最古投稿から・"在籍"でなく"居住"）**。品種の同定は `tallyVarieties`（`buildFuda` 一本化）を再利用＝ランキングと一致。集計は純関数 `computeCitizenStats(posts, catalog, hasName, now)`（`src/lib/feed/stats.ts`・`now` を引数で受け決定的）。**市民レベルは 旅人(L0=名乗り前) → 市民(L1) → 市民L2 → …市民Ln**（`citizenLevelLabel`・#272 kako-jun「古参/訪問者という語は使わない・名乗ったら市民のままレベルが上がる」。L2 は名乗り＋5投稿＋居住14日〜＝既存 `citizenLevel`）。`CitizenStats` は自分/他人共通で、catalog は動的 import（PostGrid 同型）。段階2=実績バッジ＋市民Ln 拡張、段階3=他人の公開プロフィール `/u`。
- **フロントエンドのみ**: Astro（静的生成）＋ React islands ＋ **PWA**（`@vite-pwa/astro`）。**Cloudflare Pages** にデプロイ（Workers も D1 も不要）。
- クライアントが自分で行う:
  - **鍵** — ブラウザ生成・localStorage 保存・署名（NIP-07 拡張も可）。
  - **画像アップロード** — nostr.build へ直接（NIP-98 認証）。**EXIF はサーバ側で自動削除**（身バレ＝自宅 GPS 漏れ対策が無料で効く）。維持費ほぼゼロ。
  - **投稿・購読** — Nostr リレーへ直接 publish / subscribe。
- **mypace から見える鍵＝タグ**: 投稿に `['t','mypace']` を必ず付ける（mypace タイムライン出現の条件）。詳細は §6。
- **mypace の記録 API への ping は後回し（任意・優先度低）**: ping すれば mypace の通知・スコア（ステラ/通し番号）系にも乗るが、無くても `t:mypace` だけで mypace タイムラインには出る。必要になったら将来。
- **断り書きを常設**: 「Nostr フィードを表示しているだけ・二重投稿でもコピーでもない・mypace でも見られます」。
- → mypace の正式リリースを待たず独立公開可。むしろ hanoba が mypace を賑わす燃料になる。

## 3. 実装方針

- 既存実績のある **Astro ＋ React islands ＋ PWA（`@vite-pwa/astro`）＋ Cloudflare Pages ＋ Tailwind** 構成を骨格として踏襲する（独自バックエンド／DB 層は持たない）。
- **mypace の既存コンポーザー（アップロード／クロップ／レトロ加工）を流用**する。React 同士なので島として移植する。
  - クロップ → **正方形ロック＋ドラッグで位置決め**に変更。
  - 植物写真向けの穏やかな補正 → ランダム適用でなく**選択式**。細かなスライダーは畑で使いにくいので増やさず、葉色や花色を潰さない控えめなプリセットにする。
    - **重ねがけ式プリセット**（#140/#155/#156）: 淡陽(明るさ)／翠露(S字トーン)／土香(逆S字トーン)／美華(彩度・彩度を上げるのは美華だけ)／影暮(周辺減光)／霞幻(周辺ぼかし)／線明(シャープ)。翠露＋土香 は相反するので同時選択で完全相殺。
    - **各効果は なし/弱/中/強 の4段**（#171）: プリセットは弱/中/強の3段（`levels`）を明示的に持ち、**中＝既定の効き**・線明（シャープ）は効きすぎたので中を控えめにする。チップは1回タップごとに なし→弱→中→強→なし で巡回し、**横長チップを3段階に塗りつぶして**現強度を見せる（色プリセットは持たず全部が効果トグル）。
    - **投稿時に canvas へ焼き込む**（プレビューと同じ見えを 1:1 出力に確定）。トーンカーブ(S字/逆S字)・周辺ぼかし(霞幻)・シャープ(線明)は CSS では表現しきれないため canvas のピクセル処理（LUT・blur 合成・畳み込み）で焼く。**プレビューは CSS（contrast/blur/減光オーバーレイ等）で近似**する＝畑で迷わない速さを優先し、最終の厳密さは焼き込み側に置く。実装は `src/lib/image/{presets,crop}.ts`。
    - **出力は長辺 1440px 上限に縮小（拡大はしない）・JPEG quality 0.85・フィルタは縮小後の小 canvas に焼く・複数枚は並列アップロード**（#167。nostr.build がどうせ縮小配信するため、巨大 JPEG の生成・CPU 処理・POST を短縮する。1:1 出力は不変）。
  - **下書きの自動保存・復元（#228）**: 書きかけのひとこと・写真・クロップ枠・フィルタ・並び順・選択中の写真を **IndexedDB に黙って自動保存し、開いたら黙って復元**する（「バックアップ／復旧」概念はユーザーに見せない・バナーも通知も出さない）。アップロード下書きは不変 Def でなく**状態側**なので `src/lib/composer/draft.ts`（SSR 安全・ランタイム依存ゼロのネイティブ IndexedDB 薄ラッパ）に分離し、写真 blob とメタ（本文/クロップ/フィルタ/並び/選択 id）を別ストアに持つ。本文が消えるのは ①本文右端の × ②自分で本文を空にする ③投稿成功 の3経路だけ＝**写真を選び直しても本文は保持**する（写真操作は本文に触れない）。
  - **写真の並べ替え（#274）**: 添付した複数写真（2枚以上）は、選択中の写真を **◀▶ で左/右へ1つずつ動かして順番を変えられる**（先頭＝カバー）。タップ＝編集選択は従来どおり。crop/filters は各写真（`DraftImage`）に内包されるので並べ替えは**配列入れ替えだけ**＝加工も写真と一緒に動く。並べ替えは純関数 `src/lib/composer/reorder.ts`（`moveById(items, id, delta)`・範囲外/不在は no-op・非破壊）に切り出し、Composer は `setImages` 経由で適用＝下書きの blobs(`order`)/meta(順序) effect が発火し並べ替え後の順序も自動保存・復元される。サムネ列直下に並べ替え行（`N枚目 / 全M枚`・端でボタン無効）。**◀ と ▶ はタップしやすいよう両端に離す**（中央にカウンタ・各ボタン 44px 標的）。
- **一言の `#` ハッシュタグ補完**: 入力中の `#` を**過去に使われたタグから補完**し、同じ植物のタグへ投稿が自然に集積する（emergent taxonomy＝固定の分類表を持たずタグが育つ）。補完候補の源はリレー上の過去 `t` タグ。よく使う種は Wikipedia 由来で seed 可、細かい品種名（cultivar）は Wikipedia に無いので freeform `#`（過去使用のみで補完。エンティティ紐付け `@@` の UX を似せるだけ）。既存のメンション補完 UI を流用。
- 設計のトーンは mypace に揃える（スキンであるため）。
- **PWA インストール促し（#230）**: `beforeinstallprompt` を捕捉して `preventDefault()` し event を握り、控えめなバナー「ホーム画面に追加」＋[追加]/[あとで]で `prompt()` を呼ぶ。既設置（`display-mode: standalone` / iOS `navigator.standalone`）なら出さない。iOS Safari は `beforeinstallprompt` 非対応なので未設置時だけ手動手順ヒント（共有→ホーム画面に追加）を出す。却下を localStorage に記憶し 7 日抑制する（思想は mypace 由来）。バナー先頭には**実アプリアイコン（ハノーバ市旗の H・`public/icon.svg`）を `<img>` で出す**＝ホーム画面に追加される当のアイコンを見せて「何を追加するのか」を分からせる（汎用の芽アイコンの仮置きは廃止）。状態（却下記憶・抑制判定の純関数）は `src/lib/pwa/install.ts`、表示は島 `src/components/pwa/InstallPrompt.tsx`（MainLayout に `client:idle`・window/localStorage はマウント後参照で SSR 安全）。

## 4. 主要画面

- **投稿**: 撮影/選択（最大4枚）→ 写真ごとに正方形クロップ（ドラッグ）→ 植物写真向け補正（選択式）→ 一言（必須）→ 投稿
  - **撮影とアルバムはボタンを分ける（#29）**。単一 input に `capture="environment"` を付けるとスマホでカメラ直起動が優先されアルバムから選べなくなるため、`capture` 付き input（撮影）と `capture` 無し input（アルバム）を別ボタンで持つ。
  - **必須・不足理由の a11y（#151）**: 「一言（必須）」は視覚ラベルに「（必須）」と書かず（#149）プログラム属性で支援技術に伝える＝一言 `<textarea>` に `aria-required="true"`。投稿できない理由「あと ◯◯ を入れると投稿できます」は `role="status"`＋`aria-live="polite"` で状態変化を能動読み上げ。送信ボタンは不足があるときだけ `aria-describedby` でその不足理由を指し（充足したら属性ごと外す＝dangling 参照を作らない）、disabled の理由をボタンから辿れるようにする。
- **フィード（読めるフィード・#34/#50）**: hanoba の売り＝**本文を切らず読め、1クリックも要らない**（vs Instagram）。縦並びカード（PostCard）。sm 以上＝写真左／本文中／タグ右の縦列、モバイル＝写真上／本文／タグ下。**本文テキストからは #タグ を除き**（stripHashtags）タグは右列に出す。**デスクトップはカード高さを写真の正方形（sm:h-56）に固定**し、本文・タグをその中に収める（はみ出しは clip）＝写真の下に隙間を作らない。本文かタグが clip された時だけ**「続きを読む」**（フェードは使わない）でカード全体を展開し全文＋全タグを表示。タグが多くても**1列のまま下を見切る**（2列に折り返さない・#54）。**カードの非インタラクティブ領域はどこを押しても拡大**（PostDetail モーダル・写真/リンク/タグ/「続きを読む」は個別動作を維持＝stopPropagation・写真ボタンはキーボード/SR の主導線として残す・#101）。**読み込み中はカード形のガラス pulse スケルトン**（FeedSkeleton・素っ気ない「読み込み中…」を置き換え・#99）。カード（写真）の角丸は控えめ（rounded-xl・#100）。**大画面（lg）はカード幅を広げ（max-w-5xl）、余白は主に写真を大きく**（写真 224→288px・#56）。植物タグで絞る。
- **投稿頻度の高い人をフィードで「減らす」（#138）**: 人ごとに **1/2・1/5・1/10** の間引き度合いを設定でき、その人の投稿をフィードで決定的に間引いて表示する（ミュートの手前の柔らかい手段）。判定は `hash(post.id) % level === 0`（FNV-1a・id 由来）＝**deterministic**でリロードしても残る投稿は同じ・相対順序は保つ。状態（pubkey→level）は localStorage（`src/lib/feed/dilution.ts`・SSR 安全 getLS・純関数 `diluteFeed(posts, map)`）。適用は取得済みリスト→表示の間（`PostGrid`＝フィード/discover 共通）に1段挟む。**`/me`（自分の投稿）には掛けない**＝自分を減らさない。UI は投稿詳細モーダルの著者ヘッダ下に置く `DilutionControl`。**既定は畳む**（控えめな入口・未設定は中立文言「{名前}さんの表示を調整」／設定中は「1/N に減らし中」）＝既定が「なし」なのに常駐すると「減らすのが基本」と誤解させるため。開いたときだけ **なし・1/2・1/5・1/10 の4点にスナップするスライダ**（native range＋`aria-valuetext`・中間の無意味値を出さない）が出る。見出しは**人に明示**「{名前}さんの投稿をフィードで減らす」（"薄める"は植物アプリで"植物を薄める"とも読めるため廃止）。設定すると即フィードに反映（同タブはカスタムイベント、別タブは storage で同期）。
- **トップ と みんなの植物 の住み分け（#52）**: **トップ**＝`t:hanoba` のみ（葉の場＝ローカル）。**みんなの植物（discover）既定表示**＝`#plantstr`（Nostr 全体の植物界隈）∪ `t:hanoba` の**マージ**（id 重複除去・時系列）。hanoba は `#plantstr` を強制しないが、`t:hanoba` 経由で自分の投稿も「みんな」に出る（「投稿したのに見えない」混乱の解消）。個別検索（`?q=`）は §6 の横断検索のみ。各画面に1行の住み分け説明を置く。
- **人気ランキング（#162・`/ranking`）**: 植物**品種**の人気を音楽チャート風に出す。**バックエンドレス**（§6・集計サーバを持たない）で、取得済みの **`t:hanoba` 投稿だけ**（`fetchRankingPosts`・既定 limit 500・画像ありのみ＝フィードと同母集団）を**クライアントで数える**「読むたびに数える」方式。母集団を `t:hanoba` 限定にするのは、**自分の投稿が見えてチャートを動かす＝投稿の動機**にするため（discover の横断集約は混ぜない）。集計と差分は純粋ロジック `src/lib/feed/ranking.ts`（relay も `Date.now()` も持たず `now` を引数で受ける＝テスト可能）に集約し、島 `RankingBoard` は取得・`now=Math.floor(Date.now()/1000)` の供給・描画だけを担う。
  - **集計単位＝品種**: 各投稿の `hashtags` を **`buildFuda`（§4 札・#182/#23）** にかけて品種（`{key,name(和名),sci(学名)}`）に畳み、品種ごとに投稿数を数える（`tallyVarieties`）。属/品種の畳み込み・alias の canonical 寄せ・**同一投稿内の重複 dedupe** は `buildFuda` に一本化する（同じ品種の別名を1投稿で複数付けても1票）。同一品種が複数投稿に出れば投稿ごとに加算。票数降順・同数は**初出順**で安定。
  - **週次バケット**: `isoWeekKey`（ISO 8601・**月曜始まり・UTC**・年/週番号は木曜基準。例 `2026-W25`）で投稿を週に振り分ける（`bucketByWeek`・古い→新しい）。`now` の指す週が「現在の週」。
  - **先週比（`rankWithDeltas`）**: 「直前の週」＝現在週より前で**データのある直近の過去週**（カレンダー連続でなくてよい）。**NEW**＝現在週より前のどの週にも一度も載っていない。**RE**＝過去に載ったが直前週には不在で今週復帰。**↑N/↓N/—（same）**＝直前週との順位差。**直前の過去週が無い（初週）なら全行 NEW**（偽の矢印を出さない）。NEW/RE は**取得済みの直近ウィンドウ内**（`fetchRankingPosts` 上限＝現状およそ直近 500 投稿）での判定で、全履歴に対する絶対判定ではない。**初週バナー**（「先週比は来週から」）は全行 NEW を代理にせず**データのある週数が実質 1 週以下**のときだけ出す（後発の週がたまたま新規品種ばかりでも誤って初週と出さない）。
  - **途中経過（変動）チャート（#162・uPlot）**: 順位の**表**の下に、上位 5 品種（`CHART_TOP=5`）の週次票数の推移を **1枚の折れ線チャート**で重ねる（x＝ISO 週・古い→新しい、y＝その週の票数＝整数、1品種1線・凡例＝和名）。維持者方針「変動はグラフなら uPlot のような軽量 lib を使う」に従い、**軽量・MIT・依存ゼロ・canvas の uPlot** を採用（純データは `rankRunData(posts, catalog, keys)`＝**最古〜最新の投稿週までをカレンダー上で連続させた**週列に整列した票数マトリクス・投稿の無い空週も 0 埋めで補完して線を連続させ x 軸を等間隔に保つ・連続軸は `isoWeekMondayUtc` の月曜タイムスタンプを 7 日刻みで列挙して作る＝年またぎの週番号算術を避ける・テスト可能）。uPlot は `window`/canvas に触れるので **クライアント専用＝マウント後に動的 import**（SSG ビルド中に走らせない）し、`RankRunChart` 自体も `React.lazy` で**遅延ロード**（チャートを出すときだけ uplot チャンクを取得・初期描画を軽く）。暗いシック地に合わせ軸/グリッド/ラベルはパレットの ink、**線色は global.css のトークン値だけ**（緑/オレンジ/黄/明るい葉/ink の 5 色＝`CHART_TOP=5` と一致・新色を足さない §5.1・重い塗りはしない）。canvas は装飾なので `aria-hidden`＝**意味は上の表（順位・品種・学名・件数・先週比）が正本**（行の `aria-label` は和名に学名も添える）。図には説明的 `aria-label` を付ける。reduced-motion: uPlot は既定で非アニメーション。**週が2つ未満なら退化グラフを描かず**「推移グラフは週が2つ以上たまると表示されます。」の注記に差し替える（潰れた線を見せない・RankingBoard 側でも同条件で gate）。
  - **まばらデータの作法（launch 直後の少投稿で壊れて見せない）**: 読み込み中は静かに「読み込み中…」。投稿/品種ゼロは正直に「まだランキングを出すほどの投稿が集まっていません。」＋投稿 CTA。**単週のみなら全 NEW**＋「先週比は来週から」の注記（偽の ↑↓ を出さない）。件数は小さくても正直に出す。
  - **色**: 差分バッジはパレット内（NEW/RE/↑＝緑系、↓/—＝控えめ ink）。新色は足さない（§5.1）。
  - **開発専用プレビュー（`?demo`）**: `import.meta.env.DEV` **かつ** URL に `?demo` のときだけ、合成 fixture（`src/lib/feed/ranking.demo.ts`・複数週/差分/途中経過チャートの複数系列を作り込んだダミー）を使う＝`npm run dev` でリレーに**何も publish せず**リッチ状態を確認できる。**本番ビルドでは `import.meta.env.DEV=false` でこの分岐ごとデッドコード化**（fixture も banner 文字列もバンドルに残らず、本番ページは捏造データを一切出さない）。
  - **言語別ランキングは #147（i18n）待ちで deferred**: 当面は日本語（和名＋学名）で描画する。
- **著者ヘッダ（#35・#103）**: 各カードの時刻付近に**アイコン＋ユーザー名**（kind:0 の picture/name・未取得は npub 短縮にフォールバック）。プロフィールは `fetchProfiles`（一括・`useProfiles` でキャッシュ）。`useProfiles` は **bounded retry**（最大3回・700ms間隔・取りこぼした著者だけ引き直す）で取得し、予算を使い切るまで空 Profile を恒久キャッシュしない（単発取得だと接続直後の取りこぼしを EMPTY で焼いて名前/アイコンが永久に出ず npub 生表示で固定される＝#103 デグレ。#93 の `fetchMyProfileResilient` と同じ思想を著者ヘッダ経路にも適用）。拡大モーダルにも同じ著者表示を出し、**複数サイトリンク**（kind:0 拡張 `websites[]`）はモーダル側にアイコン列で載せる（Piece 2・各人が自分のサイトへ誘導する核）。フィードカードにはリンクを出さない。サービス判定は `src/lib/profile/services.ts`（`detectServiceLabel` で URL→サービス名、`serviceIconName` で統一アイコンに対応付け、`toSiteLinks` で表示用に整形）。ブランドロゴの寄せ集めにせず意味カテゴリの統一線アイコンに寄せる（#21・X だけは普及済みで識別性が高いので専用）。各リンクは新規タブ＋`rel="noopener noreferrer"`、`title`/`aria-label` に検出ラベルを入れ識別性を担保。
- **プロフィール編集（#35 Piece3・#104）**: `/me` では **アカウント（ハンドルネーム＋変更/アカウント変更）と `ProfileEditor` を1枚のプロフィールカードに統合**する（`AccountName bare` ＋ `ProfileEditor bare` を MyGrid が1つの glass カードに内包）。ハンドルネーム変更がプロフィール内に収まり、操作ボタンは名前の下段に縦並び（モバイルで窮屈・はみ出さない）。`ProfileEditor` で**アイコン（nostr.build アップロード／URL貼付／削除）・自己紹介・複数サイト（追加/削除/並べ替え）**を編集。これで hanoba ネイティブ利用者の投稿にも著者ヘッダ（アバター/リンク）が付く。**kind:0 は replaceable** なので保存は name＋picture＋about＋websites の**全体**を publish する（`client.saveProfile`・部分更新で他項目を消さない。名前だけ変える `saveDisplayName` も付加項目をマージして載せ直す）。初期値はローカル控え（`getProfileExtra`）＋ relay の自分の kind:0（`fetchMyProfileResilient`・bounded retry）で空欄補完し、他デバイス設定も引き継ぐ。relay から非空を掴めたら**ローカル控えにも書き戻す**（表示だけ回復して控えが空のまま残ると、名前変更時の `saveDisplayName` が `websites:[]` で relay の正本を潰す＝#93 の clobber を防ぐ。`mergeProfileExtra` はローカル非空を優先）。名前は AccountName 側で設定（未設定なら保存を促す）。**秘密鍵（nsec）バックアップ（#213）**: 展開パネルの**最下部**に nsec の**表示／コピー専用**欄を置く（既定マスク・[表示]トグル・[コピー]）。ブラウザ生成の鍵は控えないと端末変更/データ消去で失われるため、その救済口。値は `exportNsec()` のローカル読み取りのみで、**kind:0（`saveProfile`）の publish payload には絶対に載せない**（表示・コピーするだけ）。`AccountName` の「アカウントを変更」(`importNsec` の貼り付け取り込み)とは別物。
- **投稿詳細（拡大モーダル）**: 写真は**元の比率のまま**見せる（クロップしない・他者著作の改変回避 #61）。複数写真は **←→ボタン／キーボード矢印／タッチスワイプ（左右）**で切り替える（#184・`photoIndex`・端は wrap・純関数 `src/lib/feed/carousel.ts` を全経路で共有。スワイプは水平優位＋しきい値40pxで縦スクロールと競合させない）。**スワイプ中はスワイプ量に応じて写真がぼける（#275）**＝ドラッグの横移動量を `swipeProgress`（[0,1]・既定 maxDx=120px）→`swipeToBlur`（既定 maxBlur=10px）に通し、画像だけを包むラッパに `filter: blur()` で当てる（←→ボタンや `ProgressiveImage` の blur-up リビールには波及させない・両 blur が合成されてもリビールが壊れない）。ドラッグ中は即追従（`transition:none`）、指を離すと 0.25s で 0 に戻り、次画像が中央に来て確定（remount で次画像のぼかしは 0 から）。**単一画像は無効・`prefers-reduced-motion` 時はぼかさない**（縦優位のドラッグも縦スクロール優先でぼかさない）。これらの写像は純関数 `carousel.ts`（`swipeProgress`/`swipeToBlur`）に切り出して市民手帳のページ遷移と共有する。hanoba 自前投稿は 1:1 出力なので正方形に、他クライアントの非正方形はその比率で表示。flex 列の中で `flex-shrink` に潰されて横長化しないよう写真枠に **`shrink-0`** を付け、自然な高さを確保する（#108）＋著者（アイコン＋名前＋サイトリンク）＋一言＋認識植物＋反応。**本文は #タグ を除いて表示**（`stripHashtags`・タグはチップに出すのでフィードカードと挙動を揃える＝二重表示しない・#43）。**植物の「札」（鉢の名前・#182/#23）は `buildFuda(post.hashtags, catalog)`** で組む＝`variety-catalog` の階層（カテゴリ→属→品種）から **学名（フォーマル）＋最も有名な和名を並べた1枚**にする（#23 学名統一。例 _Pachypodium brevicaule_ ＋「ブレビカウレ」）。`Fuda` は `{key, name(和名), sci(学名 or null), filterTags(札クリックの逆算タグ集合)}`。和名は最も具体的な著名表記＝品種名 or 属名（**属に品種があれば属単独札は畳む**・カテゴリと**非 pickable 見出し属（原種/その他塊根 等）は札にしない**＝見出し語を name に出さない）。タグは全階層だが札は具体1つ＝別概念（#181）。**同名品種の属コンテキスト解決（#223）**: 別属に同名の品種/属がありうる（例 アボカドの品種「ハス」⇔ 蓮属「ハス」、ボタンの「太陽」⇔ サボテン「太陽」）。`buildFuda` は品種タグを **その親属タグが同一投稿にある時だけ品種解決**し（`#アボカド #ハス`→アボカドのハス）、無ければ**属名一致**（`#ハス`単独→蓮属の単独札）、それも無ければ **catalog 出現順の先頭候補**に倒す（属タグ無しの素の曖昧タグはそれ以上同定しない＝「諦める」・kako-jun 決定）。同名の複数親属を同時にタグした稀ケースは**先頭1枚に dedupe**する（無関係な第2札の湧出を防ぐ）。これを成立させる前提として TagPicker は品種選択時に `#属 #品種` を併記する（後述§タグ入力）。catalog は**動的 import**（初期バンドルに載せない）＝投稿詳細の展開時、およびタイムライン/discover の**カード札用に `PostGrid` がグリッド単位で1回**読み込み各 `PostCard` へ配る（カードごとには読まない・非同期＝カードは即描画し札はロード後に出る・失敗時は札を出さないグレースフル）。**札の索引も同じくグリッドで1回だけ作って配る（#257）**: `buildFuda` を `buildVarietyIndex(catalog)`（catalog 全走査 ~2,000品種＋別名で索引を作る重い部分）と `resolveFuda(hashtags, index)`（索引を受けて1投稿を組む軽い部分）に分割し、`PostGrid` が索引を `useMemo` で1回作って各 `PostCard` に配る（旧 `buildFuda` はカード数ぶん索引を作り直していた）。索引は読み取り専用で使い回しても投稿間で汚染しない。`PostDetail`（単発）・テストは従来どおり `buildFuda(hashtags, catalog)`（＝`resolveFuda(hashtags, buildVarietyIndex(catalog))` と等価）でよい。学名 sci は **catalog の `variety.sci` を最優先 → `dictionary.ts` を品種名/属名でルックアップ → 引けなければ null**（その時は和名のみ＝グレースフル）。引けた学名はイタリック併記（#70 SciName）。札はハッシュタグと混同しないよう園芸の札（ラベル札）を模した形で出す。**投稿詳細とタイムライン/discover のカード（右列・タグの上）で同じ `FudaList` を共有**する（#239・kako-jun blink「拡大時に出る札はカードでもタグの上に出すべき」）。クリックでその品種の **discover 絞り込み**へ。**札は「札がつくルールの逆算」で複数タグに分解し AND で絞る**（#272・kako-jun「ブレビカウレ札は パキポディウム ブレビカウレ で絞るはず／ブレビカウレだけが特別ではない／毎回リセットでいい」）。各札は自分を生んだタグ集合 **`filterTags`** を持つ: 属共起で立った品種札は `[属, 品種]`（例 `/discover?tags=パキポディウム,ブレビカウレ`）、属単独札は `[属]`、親属タグの無い素の品種札は `[品種]`。`discoverTagsHref(filterTags)` が本文同形正規化（空白→`_`）して `,` 区切りで載せ、discover が AND で絞ると**必ず元投稿に当たる**（札 ⇄ タグ集合の往復）。**本文 #タグ ボタン〔単一タグ〕クリックとは区別**される（札は属＋品種で絞る）。**discover で札/タグをクリックしたら今のフィルタは置き換える**（AND で積み増すと結果が減り続けるため・複数品種 AND は上の `VarietyFilter` で明示的に組む）。
- **カードのいいね数・コメント数（#276）**: タイムライン/discover のカード（`PostGrid` 内 `PostCard`）にも**いいね数（kind:7 集計）・コメント数（リプライ集計）**を出すが、**カードは値が 1 以上のときだけ控えめに添える**（0／未ロードは要素ごと非表示）。一方**投稿詳細（`PostDetail`）は 0 でも表示**する＝**非対称**（カードは「ある時だけ」）。count はカードと `PostDetail` で同じ純関数（`countLikes`/`toComments` 系＝`countLikesByEvent`/`countCommentsByEvent`）を通すので**数え方は一致**し、**0 の出し分けだけが違う**。N+1 を避けるため `PostGrid` がグリッド単位で**1回バッチ取得**（`fetchReactionCountsBatch`/`fetchCommentCountsBatch`）し各カードへ配る（catalog/profiles と同じ『まとめ取り→配る』パターン）。
- **学名表記（#70）**: 学名は属名・種小名をイタリックにするのが正式だが、ランク略号・接続語（`var.`/`subsp.`/`ssp.`/`f.`/`cv.`/`aff.`/`cf.`/交配の `×` 等）は**直立体**にするのが植物学の正しい表記。`src/components/ui/SciName.tsx` が sci 文字列をトークン分割し接続語だけ直立で描画する。PostDetail の植物チップで使う。
- **学名データの丸め方針（#23/#182・`variety-catalog.ts` の `sci`）**: 荒い界隈の表記（Spor/PUP・自分で名乗る品種・属止まり）をどこまで正規化するかの規約。
  1. **書式＝ICNCP**: 属名大文字始まり・種小名小文字（`Pachypodium brevicaule`）。**cultivar は `Genus 'Cultivar'`**＝シングルクォート囲み・各語頭大文字・ただし冠詞/前置詞/接続詞は言語慣習で小文字（`Pierre de Ronsard`・`Perle von Nürnberg`・`Black and Blue`）。**ラテン文字に統一**（漢字 cultivar＝富貴蘭/長生蘭/さつき 等の古典銘も読みを裏取りして romaji 化＝漢字は和名 side で見せる）。
  2. **種が特定できない属通称** → `Genus sp.`（属は当て種は未特定。例 `Sansevieria sp.`・「その他塊根」の通称）。
  3. **状態語・栽培アクション**（実生/発芽/発根/仔株・Spor/PUP・spore 等）は**品種ではない**＝学名を持たせない。これらは `variety-catalog` でなく **`tag-catalog.ts` の世話/記録タグ**に置く（識別子＝品種 と 状態 は別軸＝タグ/札の分離と同じ筋）。
  4. **登録の無い趣味家銘（勝手に名乗る品種）** → 確実な学名が無ければ `Genus '通称ローマ字'` に丸める（白鯨→`Agave titanota 'Hakugei'`）。正式登録名でないものは要検証 follow-up（#182）。
  5. **グループ概念**（コケ/シダ/盆栽様式/高山植物/多年草 等＝species でない総称）は `sci` を持たない＝札は**和名のみ**（グレースフル）。
  6. 中身（綴り・読み・trade designation と cultivar の区別）の精度は #23「育てる辞書」として実運用で上げる。書式は上記で統一済み。
- **ヘッダ（全ページ共通・#30）**: 主要ナビ（みんなの植物／自分の植物／Hanōba とは／投稿する）を `SiteHeader.astro` に内蔵。sm 以上はインライン、sm 未満はハンバーガー→ドロップダウン。ページごとの `current` で現在地を強調。`hidden sm:inline` でモバイル時にリンクが消える旧構成は廃止。

## 5. デザインシステム ── 「夜の温室／大人のコレクター部屋」

### 5.0 世界観（確定・kako-jun session640）

Hanoba は明るい量販 SNS ではない。**焦げ茶の木目棚に植物が並び、オレンジの照明がぼうっと灯り、ガラスには水滴が伝う、暗くシックで高級感のある夜の部屋**。プロの植物コレクターの空間。ここに「育てて、見せる。」がある。Instagram の白い量産グリッドの対極＝大人の落ち着きと所有の喜び。

実装3原則:
1. **背景はベタ塗りにしない** ── AI 生成のぼやけた高級写真（`public/og/bg-blur.webp`）を `background-attachment: fixed` で全面に敷き、暗いオーバーレイで可読性を確保する。
2. **面（パネル）は半透明のグラス** ── `.glass` / `.glass-strong`（`backdrop-blur`＋微光の白枠）。ガラス越し・水滴・反射の空気を出す。
3. **ボタンだけは塗りでくっきり** ── CTA は `bg-ha-green`（葉）／`bg-ha-pink`（花）の塗りで可読性最優先。グラスに埋もれさせない。

### 5.1 トークン（暗色・シック）

アクセントは ToHeart 由来（**緑＝葉／ピンク＝花**）を継承するが、**ベースは「明るいクリーム」から「焦げ茶〜黒」へ刷新**。ピンクは**差し色のみ**（通知・ハイライト）。**ヘッダや地にピンク/明色を使わない**（旧クリーム地はヘッダがピンクがかって見えたため廃止）。CSS 変数は `src/styles/global.css @theme`。

```
--color-ha-green:      #6cba38   /* 葉。CTA・リンク・選択（暗地で映える明るめ） */
--color-ha-green-deep: #aee07f   /* 暗地での見出し・強調＝明るい葉色 */
--color-ha-green-soft: #20301c   /* 暗い葉色の面（写真プレースホルダ・チップ地） */
--color-ha-pink:       #ff5d6a   /* 花。差し色のみ（通知・ハイライト） */
--color-ha-pink-soft:  #3e2024   /* 暗いピンク面 */
--color-ha-orange:     #e89a4c   /* 照明＝暖色オレンジ（グロウ・ハイライト） */
--color-ha-base:       #14120d   /* ベース＝焦げ茶〜黒（背景写真の下地・グラスの奥） */
--color-ha-white:      #f6f0e6   /* 抜き＝暖かい白（ボタン上の文字・最明部） */
--color-ha-ink:        #ece6da   /* 本文＝暖かいオフホワイト（暗地に乗る） */
```

### 5.2 タイポ・モーション

- **タイポ**: 見出し＝`Outfit`（geometric sans・欧/数字）×日本語 `Zen Maru Gothic`（丸ゴシック・角丸の世界観に合わせる #170）。本文＝Zen Maru Gothic。本文400↔見出し800/900でウェイト飛躍。汎用フォント（Inter/Roboto/system/Space Grotesk）禁止（Anthropic frontend aesthetics 準拠）。`--font-display` / `--font-sans`。
- **モーション**: ページロードの控えめな staggered reveal（`.ha-rise`＋`--i`・CSS-only・`prefers-reduced-motion` で無効）。**安っぽい装飾は禁止**（ホバーでロゴを傾ける等はダサいので不可）。
- **画像リビール（#145）**: 画像は blur-up リビール（`ProgressiveImage`＋`.ha-reveal`）で出す。デコード完了まで `opacity:0` で隠し、`onLoad`（＋キャッシュ済みは `img.complete`）で `data-loaded="true"` を立てて blur＋わずかな scale を解きながらフェードインする。非プログレッシブ画像が上から帯状に描かれる生 `<img>` を置き換えたもの。`prefers-reduced-motion` では遷移なしで即時表示。4 箇所（カード写真＝object-cover 固定箱／拡大カルーセル＝object-contain／アバター＝rounded-full／あなたの植物グリッドのサムネ＝object-cover 正方形）で共有。WASM/Canvas の「現像」リビール（stage 3）は別途の将来フォローアップ。
- **フローティング UI**: 右下に「一番上へ戻る」（`ScrollToTop`・全ページ共通・400px 超で出現・`prefers-reduced-motion` 時は smooth でなく即時・#110）。**投稿 FAB（#283）**: その左隣（`PostFAB`・`right-[4.75rem]`＝隅は ScrollToTop 用に空ける固定スロット）に常駐の塗り緑 FAB（`/compose` へ1クリック・**アイコンは「横から見た綿毛」＝写真を風に放つメタファ**〔`public/post-fab.webp`・gpt-image-2 生成の白透過ラスタ。真上ビューの放射状＝＊状に紛れる `sprout`/`dandelion` を避けるための、線アイコン集 `Icon.tsx`(currentColor SVG) の例外〕・ScrollToTop の表示状態に依らず位置不動）。投稿画面（`/compose`）では `MainLayout` の `isCompose` でサーバ側に出さない。
- **綿毛の送信エフェクト（#148 / #252）**: 投稿ボタンを押すと綿毛（タンポポの種）が舞い上がる。打ち上げの一斉バーストに加え、**投稿が終わるまで少量ずつ連続スポーン**して「風に乗って次々舞い上がる」流れを作り、アップロード＋publish の長い待ち（10秒級）でも画面が止まって見えないようにする（#252）。メタファは「自分の植物の写真を風に放つ」。粒は和・水彩タッチの**透過スプライト3変種**（`public/seed-watercolor-{1..3}.png`＝黒い綿毛を白地に生成し、輝度をアルファ＋白黒反転して白い綿毛＋半透明の冠毛に変換＝クロマキーのハロ無し・API キー不要）をランダムに割り当て、大きめサイズ（~104px）＋**粒ごとの非一様スケール/skew の変形**（見かけの拡大率を混ぜる）＋横揺れ・回転ドリフト・上端フェードで一粒ずつ違う舞いにする（反復＝単調を消す）。種の生成は純関数 `lib/composer/dandelion.ts`（`makeWind`/`makeSeed`/`makeSeeds`・連続側はバッチごとに風を取り直す）、描画は `DandelionBurst`（`active={posting}` で駆動・各粒は animationend で自分を消す）＋`.ha-seed-rise`。**ボタンに残す稼働サインは綿でなく普通のスピナーリング**＋段階テキスト「写真を送信中 N/M → 投稿中」（綿はボタンから飛び出した側なので・#252）。idle の送信ボタンのアイコンは**投稿 FAB と同じ綿毛ラスタ `public/post-fab.webp`** に統一（#293・線アイコン `dandelion` から差し替え＝右下 FAB と同じ絵）。`prefers-reduced-motion` では spawn せず無音（コンポーネントが null・CSS でも `animation:none`）。`pointer-events:none`・`aria-hidden` でクリック・レイアウト・支援技術に干渉しない。**綿毛が舞ってもスクロールバーの出没で画面幅がガタつかないよう、`html` に `scrollbar-gutter: stable` で gutter を常時確保する（#294・横方向は `body{overflow-x:clip}` で切る #271 の積み残し対応）。**
- **グリッド**: 写真は Instagram explore 流に**隙間を詰め（gap 0.5）角丸は小さく（rounded-md）フラット**（浮かせない）。

### 5.3 ワードマーク／アイコン

- **ロゴ＝架空都市ハノーバの市旗（H 字）**。葉モチーフ（マンネリ）も丸も使わない。**4色に塗り分けて H に見せる**: 生成り地＋緑の左柱＋深緑の右柱＋ピンクの横棒。favicon / icon / maskable / apple-touch / ヘッダーで同型に統一（`scripts/generate-icons.mjs` で PNG 再生成）。
- **ワードマークは `Hanoba` のみ**（国際的な印象・`font-display`）。**漢字「葉の場」はロゴ下に併記しない**。カッコ表記（`Hanoba（葉の場）` 等）も使わない。
- **アイコン**: 寄せ集めの Unicode 記号（♡/×）を廃し、単一出自・統一線幅の SVG（`src/components/ui/Icon.tsx`）。**いいねは黄色い花（`flower`＝`text-ha-yellow`・#116。ハート/絵文字でなく SVG）**。投稿コンポーザの送信ボタンは綿毛ラスタ（`public/post-fab.webp`＝投稿 FAB と同じ絵・写真を風に放つ・#148/#293。投稿中はボタンを綿でなくスピナーリングに替える・#252。ナビ等の発芽双葉 `sprout`＝育てる世界観・#48 とは別アイコン）。撮影/アルバムは `camera`/`image`（#29）。情報ページ（About）導線は `info`(円＋i・#106)。サービスリンク（#35 Piece 2）も同じ線スタイルで `link`(地球)/`code`/`x`/`youtube`/`instagram`/`writing`/`art`/`music`/`shopping`/`game`/`at`/`chat` をカテゴリ単位で持つ（ブランドロゴは作らない）。**塗りの例外は `flower`(いいね)・`heart`(Ko-fi)・`x`（X 公式ロゴ・#115）・`github`（公式 Octocat・#118）**＝ブランド識別性が高いものは線縛り（#21）の明示的例外として公式グリフを塗りで使う。**ヘッダとフッタのナビは同じ並び（About→みんなの植物→あなたの植物→投稿する）に揃え、About には `info` を付ける（#106）。人気ランキング（`/ranking`・#162）はフッタのみに控えめに置く（みんなの植物の隣・共有ナビは増やしすぎない）。フッタ外部リンクは mypace=公式マーク画像 `public/mypace-icon.webp`（汎用★記号でなく mypace 独自の丸み星・#105）/GitHub=`github`（#105）。インラインのアイコン付きリンクは `leading-none` で行高を詰め、`items-center` でアイコンがテキストと光学的に揃う（#105 follow-up）。**

### 5.4 画像アセット（AI 生成・`/image`）

- `public/og/bg-blur.webp` … ぼかして暗くした全面背景（`body` の `background-image`）。
- ~~`public/og/room-dark.webp` / `public/about/{flower,field,fruit}.webp`~~ … 旧 About（三人称 SaaS 説明の 4 カード）の彩り画像。About を市民手帳（市役所ハブ・#163）へ作り替えた際にカードごと撤去し、画像も削除済み（孤児アセット整理）。
- `public/og/og-image.jpg`（1200×630）… OGP/Twitter カード。
- `public/book-frame-washi-v1.webp` / `public/book-page-washi-v1.webp` / `public/mayor-botanics-watering-can.webp` … 市民手帳（#163・#219）の和綴じ枠（`border-image` 9 スライス）・和紙ページ地・市長ボタニクスの語り手アイコン（顔は秘密＝ジョウロ）。**ロック頁（？？？）の「読めない頁」は画像でなく CSS**＝いろは歌を流し `filter: blur()` で潰した崩し字＋中央 scrim で「頁はあるが今は読めない」図鑑的示唆を出す（#219 ③・`LOCKED_PAGE_VEIL` / `LockedTeaser`・装飾なので `aria-hidden`）。**ページめくりはキーボード矢印に加えタッチスワイプでもできる（#275・左で次・右で前）＝写真カルーセルと同じ純関数 `carousel.ts`（`swipeDirection`/`swipeProgress`/`swipeToBlur`）を共有**し、**スワイプ量に応じてページ中身がぼける**（和綴じ枠は固定したまま `key={page}` の中身だけに `filter: blur()` を当て、枠ごとぼかす違和感を避ける）。前方ロック・端は no-op、指を離すと 0.25s でぼかしが戻る。`prefers-reduced-motion` 時はぼかさない（スワイプによるページ遷移自体は有効）。
- ソース PNG はリポに残さず、`/image`（codex/gpt-image-2）のプロンプトで再生成する。

### 5.5 SEO / OGP（#107）

- `MainLayout` で head を一元管理。`astro.config` の `site=https://hanoba.llll-ll.com` を基準に **canonical / og:url** をページごとの絶対 URL で出す（`Astro.url.pathname`）。`og:image`/`twitter:image` も同 site の絶対 URL。
- **JSON-LD** は `<script type="application/ld+json" is:inline>` で描画する（`<meta>` 化しない＝meta だと無効）。`WebSite` ＋ サイトリンク検索ボックス（`SearchAction` で discover の `?q=` を提示）。
- **sitemap** は `@astrojs/sitemap` が全ページから `sitemap-index.xml`＋`sitemap-0.xml` を自動生成。**`public/robots.txt`** が `Sitemap:` で参照する。
- title/description は各ページが `MainLayout` の props で渡す（未指定はデフォルト）。

### 5.6 操作（ボタン/リンク）の配置 — 統一ポリシー（#98）

バラバラな左寄せをやめ、全画面で一貫させる。

- **アクション行（フォームの送信/取消）は右寄せ（`flex justify-end`）**。**主操作＝塗りの緑ボタン（角丸 full）を右端**、副次操作（やめる/キャンセル）と補足（不足理由・保存ステータス）はその左に置く。Composer の「基本動線（主アクションを右端）」に全フォームを揃える（AccountName 取り込み・ProfileEditor 保存も右寄せに統一）。モバイルは `flex-wrap` で副次が上段に折り返してよい。
- **単一フィールドの確定**（ハンドルネーム保存など）は入力欄の右に主操作ボタンを置く（実質右寄せ）。
- **補助リンク**（モード切替「すでにアカウントを…」、追加「＋サイトを追加」）はアクション行と分け、別行・**左端**（読み始めの位置・`self-start`）で控えめに。
- **破壊的操作の確認**は中央オーバーレイ（`grid place-items-center`）に確認文＋ボタンを出す（フォームのアクション行とは別パターン。例: 投稿削除の「写真ごと削除しますか？」）。
- **単一行入力の × クリア**は共通コンポーネント `src/components/ui/ClearableInput.tsx`（`ClearableInput`＝右中央に ×）に集約。値があるとき右端に × を出し、押すと空にして再フォーカス。各単一行入力（ハンドルネーム/nsec/検索/アイコンURL/サイト）に適用。新しい単一行入力は原則この共通部品を使う。
- **複数行入力（自己紹介・ひとこと）**は共通コンポーネント `src/components/ui/ResizableTextarea.tsx` に統一（#188）。glass・右上の ×クリア・**下辺の大きなドラッグバーで高さ調整**（ポインタドラッグ＋矢印キー・既定 124/最小 104/最大 360px、`role="separator" aria-label="入力欄の高さを調整"`）を備える。高さクランプは純関数 `clampHeight` に切り出し。これで `ProfileEditor` の自己紹介と Composer のひとことが同一デザイン・同一操作感になる。キャレット制御＋ハッシュタグ補完を持つ Composer の一言（`CaptionInput`）は、この共通部品に `ref`／`onKeyDown`／補完ポップアップ（`children`）を渡して補完ロジックだけを上載せする（補完は一言専用なので共通部品には含めない）。

## 6. データ（Nostr）— 検証済み契約

mypace と同じ Nostr 空間・同じタグ規約に準拠する（独自化しない＝mypace から見える要件）。

- **イベント**: `kind: 1`（テキストノート）。
- **タグ**:
  - `['t','mypace']` … **必須・自動付与**。これで投稿は通常の mypace 投稿と同一に mypace タイムラインへ出現。
  - `['t','hanoba']` … **必須・自動付与**。hanoba のフィードはこのタグで絞る（mypace の一般投稿を hanoba に流さないため）。
  - `['client','hanoba']` … 自動付与。可視性には影響しない。
  - **植物トピック・種別は、一言（本文）にユーザーが書く `#ハッシュタグ`** で表現（例: `#アガベ #パキポ`）。本文に見えるテキストとして残す。**hanoba 側に固定の植物 taxonomy は持たない**。**本文 `#` は `t` タグ化しない**（mypace と完全一致＝独自化禁止。確定）。集約は書き込み側のタグでなく、読み取り側の二段構え検索で行う（下記）。
- **content**: 必須の一言 ＋ 画像 URL（1〜4個）を**インラインのプレーン URL**で（`https://image.nostr.build/<hash>.<ext>`。imeta タグは使わない）。**一言が空（URL のみ）は弾く**。
- **画像アップロード**: `POST https://nostr.build/api/v2/upload/files`（NIP-98 認証）。EXIF はサーバ側で自動削除。
- **リレー**: `wss://relay.damus.io`, `wss://nos.lol`（必要時に検索 `wss://search.nos.today`）。
- **鍵**: ブラウザ生成・localStorage 保存、NIP-07 任意。
- 既存コンポーザーの流用方針は §3（クロップ→1:1 ロック、レトロ加工→選択式）。
- **タグの集約（読み取り）＝二段構え**（mypace 準拠・#3/#4 で実装）: ① `{"#t":[tag]}` フィルタで `t` タグ持ちを取得、② NIP-50 `search:"#tag"` で**本文ハッシュタグ**を全文検索（他クライアントの `t` タグ無し投稿も拾う）、③ event id でマージ・重複除去。これが emergent taxonomy の本体で、書き込み側で本文 `#` を `t` 化しなくても集約できる根拠（書き込みタグは `t:mypace` / `t:hanoba` / `client:hanoba` のみ）。検索 relay = `wss://search.nos.today`。
- **discover の品種絞り込み（#239・実機 blink で多軸を全廃して再設計）**: discover は **「品種で絞るだけ」**。投稿者・期間・並び・共有・保存・本文キーワード検索・検索ボックス/探すボタンは全部廃止（kako-jun 指摘「絞り込みというより詳細検索だろ／矛盾だらけ」を受け、要らない軸を断つ）。状態は `tags: string[]`（品種＋自由タグ・AND）だけ。
  - **入力＝投稿画面と同じ `TagPicker`（filter モード）をそのまま再利用**（`VarietyFilter` でラップ）。長い品種名は打てないのでカテゴリ→属→品種のドリルダウン＋インクリメンタル検索で**選ぶ**。カタログに無い短い語は検索欄の「そのまま #◯◯ を使う」で**自由タグ**として足せる。選んだタグは AND のチップ（× で外す）。filter モード（`TagPicker mode="filter"`）は投稿専用セクション（最近/人気/世話・記録/リクエスト）を出さず、品種を選んでも **#属を前置しない（葉タグのみ）**＝AND で過剰に絞らない（属タグの無い投稿を落とさない）。選択状態は選んだタグを `#a #b` の合成 caption で TagPicker に渡して判定（既存ロジックを変えない）。
  - **取得＝`fetchDiscoverFiltered`（tags のみの filter で呼ぶ）**: tags は別名展開した `#t` の OR（#23・「パキポ」→グラキリス等）＋ NIP-50 `"#tag"`、`applyClientFilter` で軸間 AND・画像のみを確定、**新着順**（並び替えなし）。tags 空なら既定母集団 **#plantstr ∪ t:hanoba**。選んだ／外した**その場で再取得**（検索ボタンなし＝ライブ・`latestRef` で stale 破棄）。読み取り側のみで §6 契約（本文 `#` は `t` 化しない）は不変。
  - **URL = `?tags=` の deep-link**: `applyFilterToParams`/`parseFilter`（旧 `?q=`/`?tag=` は後方互換で吸収）。意図的操作は `pushState`、復元（マウント/popstate）は `replaceState`/書き込み無し（ループ防止）。
  - **SW 注意（#291・実機で2回踏んだ罠）**: PWA の Service Worker（workbox `generateSW`）は既定の `ignoreURLParametersMatching`（`utm_/fbclid` のみ）だと **`?tags=` 付きナビゲーションを precache の `/discover/index.html` にマッチさせられず**、生成 SW の navigateFallback（`createHandlerBoundToURL("/")`＝ホーム）にすり替える＝**札クリックで discover が一切出ず、ホームが描画される**（dev は SW 無しで露見しない＝必ず build→preview か本番でSW有効下を検証）。`astro.config` の `workbox.ignoreURLParametersMatching: [/.*/]` で全クエリを無視させ、クエリ付き deep-link を各ページ HTML にマッチさせる。`?q=`（JSON-LD 検索）・旧 `?tag=` も同時に救済。
  - **廃止の経緯**: 投稿者 `@名前` 解決が NIP-50 のハイフン演算子（search.nos.today は `kako-jun`→0件・`kako`→多数）で当てにならず需要も薄い／期間カレンダーは「使いたいニーズなし」かつネイティブ `<input type=date>` の区切り `/`・フォントが揃わない／並びは新着順だけでよい、として軸ごと撤去。`discoverFilter.ts`/`client.ts` に残る多軸の内部実装（author/keyword/since/until/sort・popular）は未使用化＝別 PR で掃除予定（#239 follow-up）。
- **タグ入力＝多段ドリルダウン＋検索（#22/#143/#144・実装済）**: 投稿時のタグは手打ちせず `TagPicker` から選ぶ。約 1,980 件の品種辞書を **カテゴリ→属→品種**（24 カテゴリ・近いもの順）の階層で持ち（`variety-catalog.ts`。**`TagPicker` を開いた時に動的 import で code-split**＝初期 composer バンドル非搭載・PWA precache でオフライン可。`variety-search.ts` は `catalog` を引数で受ける純関数でこの分割を保つ）、ドリルダウン（パンくず＋‹戻る・囲み外クリックで閉じる・入口は最上段）と全件横断のインクリメンタル検索（`foldForSearch`＝かな/カナ・大小・全半角を無視）で少クリック選択する。**タグと札を分ける（#181・#166 のタグ部分を撤回）**: タグ＝Twitter式ハッシュタグ＝**概要→詳細の全階層**を付けてよい。品種を選ぶと**`#属 #品種` の両方**を本文へ入れる（`findVarietyGenus` で属を引き、属→品種の順に `insertTag`・二重挿入防止で属は重複しない）。**タグは常に本文末尾へ追記し、散文とタグ群は空行（1行）で分ける**（#282＝`本文\n\n#tag …`。`insertTag` はキャレットに割り込まず末尾に積み、2個目以降のタグは同じタグ行へスペース区切りで継続＝空行は散文とタグ群の間だけ。`removeTag` で最後のタグを外すと空行ごと畳んで散文だけに戻す）。挿入後はキャレットを本文末尾へ送ってタグが見えるようにする（`focusEndSignal`・#165）。属の「このまま使う」は `#属` のみ。**カテゴリ（塊根植物/花木 等）はタグにしない**（ドリルダウンの見出し専用）。**品種チップには学名(`sci`)を小さく薄くイタリックで併記**（#200・投稿前に学名を確認でき #23「学名を厳密に書きたい層」に応える＋同名異植物の見分け。`SciName`・`aria-hidden`＝ボタンのアクセシブル名は `#品種名` のまま保つ。属/カテゴリ/世話記録タグには出さない・sci 無しのグループ概念は和名のみ）。札（鉢の名前＝具体1つ＝学名＋和名）はタグとは別概念（#182・`fuda.ts`＝`buildFuda` でハッシュタグから属＋品種を組む。`detectPlants`/`detect.ts` は撤去・§4 投稿詳細を参照）。集約は読み取り側の OR 検索（§6・別名横断）でも吸収できるが、属タグを直接付けることで属でも確実に辿れる。人気/最近で**属をタップしたら階層へ誘導**。選択済みタグは満たし色（緑）になり、**再タップで解除**（`tagsToUnpick`＝同属に兄弟が残らなければ属・カテゴリも連動して外す）。最近使ったは**投稿成功後**に本文のタグを記録（タップしただけは残さない）。世話/記録/特徴/仕立てのクイックタグ（`tag-catalog.ts`）は別枠で常設（特徴＝斑入り/綴化/石化・仕立て＝水耕/板付け 等は品種と別軸の自慢/栽培形態タグ・#221。斑の細分＝錦/黄斑 等は「斑入り」へ一本化・#251。分類語でないので #166 非抵触）。**入る値は本文 `#` テキストのみ＝`t` 化しない**（§6 契約不変）。旧 `PlantSuggest`/`childrenOf`（#63/#23 Phase2 の俗称検出・「もっと具体的に」）はこのドリルダウン＋検索に置換し撤去。クイックタグ（世話/記録/特徴/仕立て）は**カテゴリ内を作業順/時系列に並べ**（#251 で頻度順から変更）、先頭 `INLINE_LIMIT` 件を常時インライン表示する。あふれが `MIN_OVERFLOW` 件以上ある行だけ行末「その他」で残り全件を見せ、それ未満の行は全件インライン（＝**世話/記録だけ「その他」が付き、特徴/仕立ては全インライン**＝『下のカテゴリにその他が無い／開いても数件』のガタつきを解消・#251）。ポップアップは**画面中央の不透明モーダル**（portal＋scrim＋glass-strong・×/Esc/scrim で閉じる・#243＝旧・トリガー半重ねの absolute は廃止）で**あふれ分だけ**見せ、インライン済みの定番は重複させない（#186）。発芽は世話へ移し 発芽→発根→実生 を隣接、記録から発芽を削除（#169）。**タグに載せる/載せない の2原則（#251・kako-jun 実機レビュー）**: ①症状・トラブル・失敗はタグにしない（本文に書く＝徒長/葉焼け/病気/害虫 等を排除）／②現実の家・私生活を指すタグ禁止（ハノーバ＝架空都市の世界観内＝『我が家の植物』を排除）。写真追加「＋」のポップアップ（ImagePicker compact）も同様にトリガーに半重ねで出す（#187）。タグ（品種）が無い植物は「追加をリクエスト」導線から **`/vote` の「品種への要望」板**（住民投票 BBS の先頭・Nostalgic）へ送る（#232・新規タブで開き合成中の下書きを保つ）。**旧 GitHub Issue 起票は廃止**＝一般ユーザーを GitHub に飛ばさない。品種に関する要望（並び順・追加・その他）はこの板に集約する。
- **植物の別名 OR 検索（#23 Phase 2・実装済）**: タグ軸が辞書の植物に解決できる場合（`tagAliasValues`＝`findPlantByTerm`/`plantTagValues`）、その植物の**全表記を横断**する。多軸エンジン `fetchDiscoverFiltered`（#131）が `#t` を別名展開して OR 取得（`{"#t":[名前, 別名, 単語学名…]}`）し、`applyClientFilter` の軸間 AND もタグごとに**別名集合で照合**する（例: タグ「パキポ」が `#グラキリス` 投稿に一致）。PostDetail の植物チップはそのタグに絞り込み（`#<著名名>`→tags 軸）この経路に乗る。
- **コメント＝Nostr リプライ（#142・実装済）**: 投稿へのコメントを **NIP-10 のリプライ（`kind:1`）** として表現する（投稿詳細モーダルの `CommentSection`）。これは表示専用だったいいね（kind:7 集計）に対する**書き込み側の相棒**＝hanoba から Nostr に「足す」もう一つの経路。
  - **タグ**: `["e", <親投稿id>, "", "root"]`（親投稿を root に印付ける direct reply。relay ヒントは空）＋ `["t","mypace"]` ＋ `["client","hanoba"]`。
  - **`p` タグ（@呼びかけ）は付けない**: コメントは投稿者を名指しで呼び出す通知でなく「その投稿にそっと添える一言」（静かな観賞 SNS の posture・確定）。
  - **`t:hanoba` は付けない**: hanoba フィード/人気タグ集計は `#t:hanoba` で絞って**画像投稿だけ**を集める（`fetchHanobaFeed` / `fetchPopularHashtags`）。文章だけのコメントに `t:hanoba` を付けるとフィードのノイズ・タグ集計の汚染になる。コメントは親の `e` タグ経由（`{kinds:[1], "#e":[postId]}`＝`fetchReplies`）で読むので不要。
  - **読み取り**: `{kinds:[1], "#e":[postId]}`（`fetchReplies`）。このフィルタは親を `e` タグで参照する kind:1 を**全部**返すので、引用リポスト（NIP-18・`["e", postId, "", "mention"]`）が混ざる。`toComments(events, postId)` は**本物のリプライだけを残す**＝「値が `postId` に一致し marker（`tag[3]`）が `"mention"` でない `e` タグ」を1つでも持つもの（`"root"`/`"reply"`/マーカー無し＝残す、`"mention"`＝引用リポストは落とす）。あわせてリレー間の重複は id で除去。表示は古い順/新しい順を切り替え（`sortComments`）。**削除**は投稿と同じく NIP-09 `kind:5`（`deleteComment`。コメントは画像を持たないので実体削除は無い）。
  - ビルダーは `buildReplyTemplate`（純関数・空 content / 空 parentId は throw）、relay 呼び出しは `client.ts`（`fetchReplies` / `publishReply` / `deleteComment`）に集約（§3）。本文 `#` を `t` 化しない §6 契約は不変。
- **住民投票＝Nostalgic BBS 埋め込み（#160・`/vote`）**: 市役所ハブ（#163）の最初に開庁する役所。投票（意見集約）は **kako-jun 自作のバックエンドレス掲示板 Nostalgic BBS**（外部ホスト・`https://nostalgic.llll-ll.com`）を web component で埋める＝hanoba 側にサーバ/DB を足さない（§7 non-goal 維持）。
  - **埋め込み規約**: web component `<nostalgic-bbs id="..." theme="dark" width="100%">` ＋ ローダー `https://nostalgic.llll-ll.com/components/bbs.js`。`theme="dark"` が暗色シック（§5）に馴染む。`/vote` は専用 1 ページなのでローダーは `<script is:inline src=... defer>` で **1 度だけ**読む（多ページ footer の冪等注入は不要）。Astro は外部 src をバンドルせず、component はクライアントで upgrade（板＋投稿フォームを描画）＝SSG を壊さない。板の実描画・dark テーマ・投稿動線は**実ブラウザでのみ確認できる**（jsdom/build では見えない）。
  - **3 板（公開 embed id）**: 品種への要望 `hanoba-16b82341`（**品種に関する要望は全部ここ**＝並び順・名簿に無い品種の追加・その他。TagPicker #169/#232 の「追加をリクエスト」もこの板へ集約＝GitHub 廃止。先頭＝一番上に置く）／欲しい機能 `hanoba-76cf0aed`（機能リクエスト）／バグ報告 `hanoba-12e8672f`（不具合報告）。各板は glass 列で囲み（§5.0 原則2）、見出し＝h2・節は `aria-label` 付き。
  - **開放範囲**: `/vote` は**全員に開放**（市民レベルのゲートなし）。ハブからは L1+ が辿るが、直接アクセス・投稿は無制限（名前は任意）。

## 7. Non-goals

- 自前バックエンド／DB を持たない。
- 動画。
- 植物同定・図鑑（GreenSnap／Planta とは戦わない。こちらは SNS）。

## 8. Open

- クロスクライアント二段構え検索（§6 の `#t` フィルタ＋NIP-50 `search`）は**将来の discover 機能で実装**する（#4 の hanoba 限定フィードでは使わない）。#4 は `t:hanoba` で絞った hanoba 投稿だけを表示し、タグ絞り込みも取得済み hanoba 投稿のクライアント側フィルタに留める（他クライアント投稿を混ぜない）。
- （済）本文 `#` の扱い＝**`t` タグ化しない**・集約は二段構えの読み取り検索（§6・mypace 準拠・確認済）。hanoba も content `#` をテキストとして踏襲し、ナビ可能タグとして扱う。
- ハッシュタグ補完の seed 範囲（Wikipedia 由来の種名をどこまで持つか、species に `@@` エンティティ紐付けを使うか。cultivar は freeform `#`）。
- レトロ加工に加え「即時の軽い補正（彩度等）」を別途持つかの線引き。
- サブドメイン `hanoba.llll-ll.com`。
- mypace 記録 API への ping（**後回し・任意**。通知/スコア参加が要れば将来）。
- （済）パレット §5・Nostr 契約 §6・タグ方針（自動＝`t:mypace`＋`t:hanoba`、トピックは本文 `#`）。
