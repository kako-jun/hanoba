// クイックタグ＝非分類の世話/記録/特徴/仕立てラベル（#22 / #169 / #221）。
//
// 植物の分類（カテゴリ→属→品種）は variety-catalog.ts（#143）へ移し、TagPicker は
// それを動的 import でドリルダウンする（#144）。ここに残すのは taxonomy ではない
// 「水やり」「開花」等の世話/記録に加え、品種と別軸の特徴（斑入り等）/仕立て（水耕等）＝
// 投稿で高頻度に使う小さなクイックセット（#221）。いずれも分類語(#166)でなく特徴・方法。
//
// 共通化が核心（#169）: 皆が同じ定番タグを使えば集計・検索が効く。各行の `tags` は
// **頻度順**（よく使う概念ほど先頭）に並べ、定番を網羅する（捏造でなく一般的な園芸用語）。
// TagPicker は先頭 N 件（INLINE_LIMIT）だけ常時インライン表示し、あふれは行末の
// 「その他」ポップアップで**全件**見せる（幅でクロップして候補を消さない）。
// → 境界（インライン/ポップアップ）はデータでなく TagPicker 側の表示ロジックで決める。
//   ここは「頻度上位が先頭」という並びだけ保証し、`more` 等は持たない。
//
// 並びの規約:
// - propagation（繁殖）系は **発芽 → 発根 → 実生** の自然な順で**隣接**させる（世話の中・#169）。
//   発芽は記録から世話へ移し、記録には置かない（二重カウント防止）。
// - 「発根管理」のような世話「発根」との近接同義は片方（発根）へ寄せて重複させない。
// - 分類語（多肉植物 等）は入れない（#166＝分類はタグにしない）。
//
// 値は「タグとして本文に入る文字列」（スペースは入力側で _ に正規化する）。

export interface TagCategory {
  /** 見出し（カテゴリ名）。 */
  label: string;
  /** そのカテゴリの代表タグ（頻度順・タップで本文へ挿入）。先頭 N をインライン・全件をポップアップに出す。 */
  tags: string[];
}

export const TAG_CATEGORIES: TagCategory[] = [
  {
    // 栽培アクション（頻度高い順）。発芽→発根→実生 を隣接させる（#169）。
    label: "世話",
    tags: [
      "水やり",
      "植替え",
      "用土",
      "肥料",
      "剪定",
      "発芽",
      "発根",
      "実生",
      "挿し木",
      "株分け",
      "葉焼け",
      "徒長",
      "遮光",
      "断水",
      "殺虫",
      "うどんこ病",
      "根腐れ",
      "仕立て直し",
    ],
  },
  {
    // できごと・状態（頻度高い順）。発芽は世話へ移したのでここには置かない（#169）。
    label: "記録",
    tags: [
      "開花",
      "新葉",
      "蕾",
      "成長記録",
      "入手",
      "お迎え",
      "結実",
      "紅葉",
      "我が家の植物",
      "棚",
      "開花待ち",
      "休眠",
      "ビフォーアフター",
    ],
  },
  {
    // 品種と別軸の特徴（自慢系・頻度高い順）。斑入りを先頭に。これらは分類でなく
    // 個体の特徴なので #166 に抵触しない（#221）。綴化=てっか、石化/モンスト=多肉の自慢系、
    // 錦/黄斑/白斑/中斑/覆輪 は斑の種類。
    label: "特徴",
    tags: [
      "斑入り",
      "綴化",
      "石化",
      "錦",
      "黄斑",
      "白斑",
      "中斑",
      "覆輪",
      "モンスト",
    ],
  },
  {
    // 仕立て・栽培形態（頻度高い順）。水耕を先頭に。栽培方法であって分類でない（#221）。
    // 板付け=ビカク、着生、ベアルートは入手/管理形態の定番。
    // 「苔玉」は variety-catalog にコケ品種「苔玉」が実在し buildFuda で札化＝ranking を汚染するため
    // ここには置かない（横断タグは札/集計に出さない方針）。苔玉はコケ品種側で表現する。
    label: "仕立て",
    tags: [
      "水耕",
      "水挿し",
      "ハイドロカルチャー",
      "板付け",
      "着生",
      "ベアルート",
    ],
  },
];
