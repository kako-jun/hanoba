// クイックタグ＝非分類の世話/記録ラベル（#22）。
//
// 植物の分類（カテゴリ→属→品種）は variety-catalog.ts（#143）へ移し、TagPicker は
// それを動的 import でドリルダウンする（#144）。ここに残すのは taxonomy ではない
// 「水やり」「開花」等＝投稿で高頻度に使う世話/記録の小さなクイックセットだけ。
// これらは件数が少なく毎回使うので、ドリルダウンの外に eager に常時表示する。
//
// 値は「タグとして本文に入る文字列」（スペースは入力側で _ に正規化する）。

export interface TagCategory {
  /** 見出し（カテゴリ名）。 */
  label: string;
  /** そのカテゴリの代表タグ（タップで本文へ挿入）。 */
  tags: string[];
}

export const TAG_CATEGORIES: TagCategory[] = [
  {
    label: "世話",
    tags: [
      "水やり",
      "植替え",
      "用土",
      "発根",
      "実生",
      "株分け",
      "葉焼け",
      "徒長",
      "遮光",
      "仕立て直し",
    ],
  },
  {
    label: "記録",
    tags: ["発芽", "新葉", "蕾", "開花", "結実", "成長記録", "入手", "棚", "我が家の植物"],
  },
];
