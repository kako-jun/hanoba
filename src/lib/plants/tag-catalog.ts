// クイックタグ＝非分類の 投稿の種類/世話/記録/特徴/仕立てラベル（#22 / #169 / #221 / #251 / #311）。
//
// 植物の分類（カテゴリ→属→品種）は variety-catalog.ts（#143）へ移し、TagPicker は
// それを動的 import でドリルダウンする（#144）。ここに残すのは taxonomy ではない
// 「質問/失敗」（投稿の種類）・「水やり」「開花」等の世話/記録に加え、品種と別軸の特徴
// （斑入り等）/仕立て（水耕等）＝投稿で高頻度に使う小さなクイックセット（#221）。
// いずれも分類語(#166)でなく投稿タイプ・特徴・方法。
//
// タグに載せる / 載せない の2原則（#251・kako-jun 実機レビュー。#311 で原則1を細分）:
//   1. **症状の細目タグは不可／投稿の種類を表す単一タグ（質問・失敗）は可**（#311）。
//      → 徒長・葉焼け・殺虫・うどんこ病・根腐れ のような**症状名**はここに入れない
//      （「枯らした」「虫がついた」は一言で書けば足りる＝粒が細かすぎ集計を壊す）。
//      一方で **「質問」「失敗」＝投稿の種類**（その2文字自体がタグ）は載せる＝将来
//      「質問だけ／失敗だけのタイムライン」を作る余地を残す（投稿タイプの単一タグ・1個）。
//      症状の細目（徒長/葉焼け…）と投稿タイプ（質問/失敗）を別扱いにするのが要点。
//   2. 現実の家・個人・私生活を指すタグ禁止。ハノーバ（架空都市）の世界観の内側だけ。
//      → 「我が家の植物」のような現実の家を持ち込む語は入れない。
//
// 共通化が核心（#169）: 皆が同じ定番タグを使えば集計・検索が効く。特定の病名や、個体ごとの
// 細かな斑の種類のように粒が細かすぎる語は各自バラバラに付いて集計を壊すので畳む
// （斑の細分＝錦/黄斑/白斑/中斑/覆輪 は「斑入り」一本へ・#251）。
//
// 並びの規約（#251 で頻度順→作業順/時系列に変更）:
// - 各行の `tags` は **作業順（世話）/ 時系列（記録）** に並べ、頭から自然に辿れるようにする。
// - propagation（繁殖）系は **発芽 → 発根 → 実生** の自然な順で**隣接**させる（#169）。
//   発芽は記録でなく世話に置く（二重カウント防止）。
// - 分類語（多肉植物 等）は入れない（#166＝分類はタグにしない）。
// - TagPicker は先頭 N 件（INLINE_LIMIT）だけ常時インライン表示し、あふれが十分多いときだけ
//   行末「その他」ポップアップで全件見せる（境界は TagPicker 側の表示ロジックで決める）。
//   ここは「作業順/時系列で並んでいる」ことだけ保証し、`more` 等は持たない。
//
// 値は「タグとして本文に入る文字列」（スペースは入力側で _ に正規化する）。

export interface TagCategory {
  /** 見出し（カテゴリ名）。 */
  label: string;
  /** そのカテゴリの代表タグ（作業順/時系列・タップで本文へ挿入）。先頭 N をインライン・残りをポップアップに出す。 */
  tags: string[];
}

export const TAG_CATEGORIES: TagCategory[] = [
  {
    // 投稿の種類（#311・kako-jun 追補）。世話/記録/特徴/仕立てとは別軸＝「この投稿が何か」を表す単一タグ。
    // 成長記録＝育ちを記録する投稿、実験＝新しい栽培法/繁殖を試す投稿、質問＝助言を求める投稿、
    // 失敗＝枯らした/しくじった共有。いずれも「投稿が何か」＝投稿タイプ（開花/結実 のような植物の
    // 出来事/状態とは軸が違う・kako-jun「成長記録とか実験も投稿の種類」）。将来「質問だけ／失敗だけ／
    // 成長記録だけ の TL」を作る余地。症状の細目（徒長/葉焼け…）はタグにしない（原則1・本文へ）。
    // overflow しない件数なので素の行で先頭に出す（高位の descriptor・共有文化を促す）。
    label: "投稿の種類",
    // 並び（#311 follow-up・kako-jun）: 成長記録/質問（前向き）→ 実験/失敗（挑戦・隣接）。
    // 「実験は失敗の一つ左」＝実験と失敗を隣接させる（試して→しくじる の流れ）。
    tags: [
      "成長記録",
      "質問",
      "実験",
      "失敗",
    ],
  },
  {
    // 前向きな世話・繁殖アクション（作業順）。土まわり → 日常管理 → 整える → 繁殖。
    // 症状・トラブル（徒長/葉焼け/殺虫/うどんこ病/根腐れ）は載せない（原則1・本文へ書く）。
    label: "世話",
    tags: [
      "用土",
      "植替え",
      "水やり",
      "肥料",
      "遮光",
      "断水",
      "剪定",
      "仕立て直し",
      "発芽",
      "発根",
      "実生",
      "挿し木",
      "株分け",
    ],
  },
  {
    // 植物に起きた**できごと・状態**（時系列）。手に入れる → 育つ → 咲く → 実る → 休む。
    // 「成長記録」「実験」は「投稿が何か」＝投稿の種類へ移した（#311 追補）＝ここは植物の出来事だけ。
    // 「我が家の植物」は現実の家なので載せない（原則2）。
    label: "記録",
    tags: [
      "入手",
      "お迎え",
      "新葉",
      "蕾",
      "開花",
      "結実",
      "収穫",
      "紅葉",
      "休眠",
      "棚",
      "ビフォーアフター",
    ],
  },
  {
    // 品種と別軸の自慢系の特徴（#221）。斑の細分は「斑入り」一本へ畳む（#251）。
    // 綴化＝てっか、石化 は斑とは別軸の変異。分類でなく個体の特徴なので #166 に抵触しない。
    label: "特徴",
    tags: [
      "斑入り",
      "綴化",
      "石化",
    ],
  },
  {
    // 仕立て・栽培形態（#221）。水耕を先頭に。栽培方法であって分類でない。
    // 並びは意味の近いものを隣接させる（#311 kako-jun レビュー）: 水耕系の栽培形態
    // （水耕→ハイドロカルチャー）を隣接 → 水挿し（水で発根＝繁殖寄り）→ 着生仕立て
    // （板付け＝ビカク・着生）→ ベアルート（入手/管理形態）。
    // 「苔玉」は variety-catalog にコケ品種「苔玉」が実在し buildFuda で札化＝ranking を汚染するため
    // ここには置かない（横断タグは札/集計に出さない方針）。苔玉はコケ品種側で表現する。
    label: "仕立て",
    tags: [
      "水耕",
      "ハイドロカルチャー",
      "水挿し",
      "板付け",
      "着生",
      "ベアルート",
    ],
  },
];
